// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/runtime_combo/runtime_combo.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.runtime_combo";

/** Tri-state override for the global slow-release setting. */
export const SlowReleaseOverride = {
  SLOW_RELEASE_OVERRIDE_INHERIT: 0,
  SLOW_RELEASE_OVERRIDE_ON: 1,
  SLOW_RELEASE_OVERRIDE_OFF: 2,
  UNRECOGNIZED: -1,
} as const;

export type SlowReleaseOverride = typeof SlowReleaseOverride[keyof typeof SlowReleaseOverride];

export namespace SlowReleaseOverride {
  export type SLOW_RELEASE_OVERRIDE_INHERIT = typeof SlowReleaseOverride.SLOW_RELEASE_OVERRIDE_INHERIT;
  export type SLOW_RELEASE_OVERRIDE_ON = typeof SlowReleaseOverride.SLOW_RELEASE_OVERRIDE_ON;
  export type SLOW_RELEASE_OVERRIDE_OFF = typeof SlowReleaseOverride.SLOW_RELEASE_OVERRIDE_OFF;
  export type UNRECOGNIZED = typeof SlowReleaseOverride.UNRECOGNIZED;
}

/** Where a combo's currently effective configuration comes from. */
export const ComboSource = {
  /** COMBO_SOURCE_EMPTY - No stored value and no compile-time default. */
  COMBO_SOURCE_EMPTY: 0,
  /** COMBO_SOURCE_DEFAULT - A compile-time default, not overridden at runtime. */
  COMBO_SOURCE_DEFAULT: 1,
  /**
   * COMBO_SOURCE_OVERRIDDEN - A compile-time default exists, but a stored runtime value takes
   * precedence (which may itself be a disabled tombstone).
   */
  COMBO_SOURCE_OVERRIDDEN: 2,
  /** COMBO_SOURCE_RUNTIME - A stored runtime value with no compile-time default. */
  COMBO_SOURCE_RUNTIME: 3,
  UNRECOGNIZED: -1,
} as const;

export type ComboSource = typeof ComboSource[keyof typeof ComboSource];

export namespace ComboSource {
  export type COMBO_SOURCE_EMPTY = typeof ComboSource.COMBO_SOURCE_EMPTY;
  export type COMBO_SOURCE_DEFAULT = typeof ComboSource.COMBO_SOURCE_DEFAULT;
  export type COMBO_SOURCE_OVERRIDDEN = typeof ComboSource.COMBO_SOURCE_OVERRIDDEN;
  export type COMBO_SOURCE_RUNTIME = typeof ComboSource.COMBO_SOURCE_RUNTIME;
  export type UNRECOGNIZED = typeof ComboSource.UNRECOGNIZED;
}

export interface BehaviorBinding {
  behaviorId: number;
  param1: number;
  param2: number;
}

export interface Combo {
  index: number;
  name: string;
  keyPositions: number[];
  behavior: BehaviorBinding | undefined;
  layerMask: number;
  enabled: boolean;
  /** 0 means inherit the global timeout. */
  timeoutMs: number;
  /** 0 means inherit the global require-prior-idle setting. */
  requirePriorIdleMs: number;
  slowReleaseOverride: SlowReleaseOverride;
  source: ComboSource;
}

export interface GlobalSettings {
  timeoutMs: number;
  slowRelease: boolean;
  maxCombo: number;
  /** 0 disables the require-prior-idle guard. */
  requirePriorIdleMs: number;
}

export interface ListCombosRequest {
}

export interface GetComboRequest {
  index: number;
}

export interface SetComboRequest {
  index: number;
  keyPositions: number[];
  behavior: BehaviorBinding | undefined;
  layerMask: number;
  enabled: boolean;
  persist: boolean;
  /** 0 means inherit the global timeout. */
  timeoutMs: number;
  /** 0 means inherit the global require-prior-idle setting. */
  requirePriorIdleMs: number;
  slowReleaseOverride: SlowReleaseOverride;
}

export interface SetComboNameRequest {
  index: number;
  name: string;
  persist: boolean;
}

export interface DeleteComboRequest {
  index: number;
  persist: boolean;
}

/**
 * Erases a stored runtime override, restoring the compile-time default (or an
 * empty slot, if there is none).
 */
export interface ResetComboRequest {
  index: number;
}

export interface GetGlobalSettingsRequest {
}

export interface SetTimeoutMsRequest {
  timeoutMs: number;
  persist: boolean;
}

export interface SetSlowReleaseRequest {
  slowRelease: boolean;
  persist: boolean;
}

export interface SetRequirePriorIdleMsRequest {
  requirePriorIdleMs: number;
  persist: boolean;
}

export interface SaveRequest {
}

export interface DiscardRequest {
}

export interface Request {
  listCombos?: ListCombosRequest | undefined;
  getCombo?: GetComboRequest | undefined;
  setCombo?: SetComboRequest | undefined;
  setComboName?: SetComboNameRequest | undefined;
  deleteCombo?: DeleteComboRequest | undefined;
  getGlobalSettings?: GetGlobalSettingsRequest | undefined;
  setTimeoutMs?: SetTimeoutMsRequest | undefined;
  setSlowRelease?: SetSlowReleaseRequest | undefined;
  save?: SaveRequest | undefined;
  discard?: DiscardRequest | undefined;
  setRequirePriorIdleMs?: SetRequirePriorIdleMsRequest | undefined;
  resetCombo?: ResetComboRequest | undefined;
}

export interface ListCombosResponse {
  combos: Combo[];
}

export interface GetComboResponse {
  combo: Combo | undefined;
}

export interface GetGlobalSettingsResponse {
  settings: GlobalSettings | undefined;
}

export interface StatusResponse {
  affectedCount: number;
  message: string;
}

export interface ErrorResponse {
  message: string;
}

export interface Response {
  error?: ErrorResponse | undefined;
  listCombos?: ListCombosResponse | undefined;
  getCombo?: GetComboResponse | undefined;
  status?: StatusResponse | undefined;
  getGlobalSettings?: GetGlobalSettingsResponse | undefined;
}

function createBaseBehaviorBinding(): BehaviorBinding {
  return { behaviorId: 0, param1: 0, param2: 0 };
}

export const BehaviorBinding: MessageFns<BehaviorBinding> = {
  encode(message: BehaviorBinding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.behaviorId !== 0) {
      writer.uint32(8).uint32(message.behaviorId);
    }
    if (message.param1 !== 0) {
      writer.uint32(16).uint32(message.param1);
    }
    if (message.param2 !== 0) {
      writer.uint32(24).uint32(message.param2);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BehaviorBinding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBehaviorBinding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.behaviorId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.param1 = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.param2 = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<BehaviorBinding>): BehaviorBinding {
    return BehaviorBinding.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BehaviorBinding>): BehaviorBinding {
    const message = createBaseBehaviorBinding();
    message.behaviorId = object.behaviorId ?? 0;
    message.param1 = object.param1 ?? 0;
    message.param2 = object.param2 ?? 0;
    return message;
  },
};

function createBaseCombo(): Combo {
  return {
    index: 0,
    name: "",
    keyPositions: [],
    behavior: undefined,
    layerMask: 0,
    enabled: false,
    timeoutMs: 0,
    requirePriorIdleMs: 0,
    slowReleaseOverride: 0,
    source: 0,
  };
}

export const Combo: MessageFns<Combo> = {
  encode(message: Combo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    writer.uint32(26).fork();
    for (const v of message.keyPositions) {
      writer.uint32(v);
    }
    writer.join();
    if (message.behavior !== undefined) {
      BehaviorBinding.encode(message.behavior, writer.uint32(34).fork()).join();
    }
    if (message.layerMask !== 0) {
      writer.uint32(48).uint32(message.layerMask);
    }
    if (message.enabled !== false) {
      writer.uint32(64).bool(message.enabled);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(72).uint32(message.timeoutMs);
    }
    if (message.requirePriorIdleMs !== 0) {
      writer.uint32(80).uint32(message.requirePriorIdleMs);
    }
    if (message.slowReleaseOverride !== 0) {
      writer.uint32(88).int32(message.slowReleaseOverride);
    }
    if (message.source !== 0) {
      writer.uint32(96).int32(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Combo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCombo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.keyPositions.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.keyPositions.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.behavior = BehaviorBinding.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.layerMask = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.requirePriorIdleMs = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.slowReleaseOverride = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Combo>): Combo {
    return Combo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Combo>): Combo {
    const message = createBaseCombo();
    message.index = object.index ?? 0;
    message.name = object.name ?? "";
    message.keyPositions = object.keyPositions?.map((e) => e) || [];
    message.behavior = (object.behavior !== undefined && object.behavior !== null)
      ? BehaviorBinding.fromPartial(object.behavior)
      : undefined;
    message.layerMask = object.layerMask ?? 0;
    message.enabled = object.enabled ?? false;
    message.timeoutMs = object.timeoutMs ?? 0;
    message.requirePriorIdleMs = object.requirePriorIdleMs ?? 0;
    message.slowReleaseOverride = object.slowReleaseOverride ?? 0;
    message.source = object.source ?? 0;
    return message;
  },
};

function createBaseGlobalSettings(): GlobalSettings {
  return { timeoutMs: 0, slowRelease: false, maxCombo: 0, requirePriorIdleMs: 0 };
}

export const GlobalSettings: MessageFns<GlobalSettings> = {
  encode(message: GlobalSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeoutMs !== 0) {
      writer.uint32(8).uint32(message.timeoutMs);
    }
    if (message.slowRelease !== false) {
      writer.uint32(16).bool(message.slowRelease);
    }
    if (message.maxCombo !== 0) {
      writer.uint32(24).uint32(message.maxCombo);
    }
    if (message.requirePriorIdleMs !== 0) {
      writer.uint32(32).uint32(message.requirePriorIdleMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GlobalSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGlobalSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.slowRelease = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxCombo = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requirePriorIdleMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GlobalSettings>): GlobalSettings {
    return GlobalSettings.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GlobalSettings>): GlobalSettings {
    const message = createBaseGlobalSettings();
    message.timeoutMs = object.timeoutMs ?? 0;
    message.slowRelease = object.slowRelease ?? false;
    message.maxCombo = object.maxCombo ?? 0;
    message.requirePriorIdleMs = object.requirePriorIdleMs ?? 0;
    return message;
  },
};

function createBaseListCombosRequest(): ListCombosRequest {
  return {};
}

export const ListCombosRequest: MessageFns<ListCombosRequest> = {
  encode(_: ListCombosRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCombosRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCombosRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListCombosRequest>): ListCombosRequest {
    return ListCombosRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListCombosRequest>): ListCombosRequest {
    const message = createBaseListCombosRequest();
    return message;
  },
};

function createBaseGetComboRequest(): GetComboRequest {
  return { index: 0 };
}

export const GetComboRequest: MessageFns<GetComboRequest> = {
  encode(message: GetComboRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetComboRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetComboRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetComboRequest>): GetComboRequest {
    return GetComboRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetComboRequest>): GetComboRequest {
    const message = createBaseGetComboRequest();
    message.index = object.index ?? 0;
    return message;
  },
};

function createBaseSetComboRequest(): SetComboRequest {
  return {
    index: 0,
    keyPositions: [],
    behavior: undefined,
    layerMask: 0,
    enabled: false,
    persist: false,
    timeoutMs: 0,
    requirePriorIdleMs: 0,
    slowReleaseOverride: 0,
  };
}

export const SetComboRequest: MessageFns<SetComboRequest> = {
  encode(message: SetComboRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    writer.uint32(18).fork();
    for (const v of message.keyPositions) {
      writer.uint32(v);
    }
    writer.join();
    if (message.behavior !== undefined) {
      BehaviorBinding.encode(message.behavior, writer.uint32(26).fork()).join();
    }
    if (message.layerMask !== 0) {
      writer.uint32(40).uint32(message.layerMask);
    }
    if (message.enabled !== false) {
      writer.uint32(56).bool(message.enabled);
    }
    if (message.persist !== false) {
      writer.uint32(64).bool(message.persist);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(72).uint32(message.timeoutMs);
    }
    if (message.requirePriorIdleMs !== 0) {
      writer.uint32(80).uint32(message.requirePriorIdleMs);
    }
    if (message.slowReleaseOverride !== 0) {
      writer.uint32(88).int32(message.slowReleaseOverride);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetComboRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetComboRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.keyPositions.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.keyPositions.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.behavior = BehaviorBinding.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.layerMask = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.requirePriorIdleMs = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.slowReleaseOverride = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetComboRequest>): SetComboRequest {
    return SetComboRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetComboRequest>): SetComboRequest {
    const message = createBaseSetComboRequest();
    message.index = object.index ?? 0;
    message.keyPositions = object.keyPositions?.map((e) => e) || [];
    message.behavior = (object.behavior !== undefined && object.behavior !== null)
      ? BehaviorBinding.fromPartial(object.behavior)
      : undefined;
    message.layerMask = object.layerMask ?? 0;
    message.enabled = object.enabled ?? false;
    message.persist = object.persist ?? false;
    message.timeoutMs = object.timeoutMs ?? 0;
    message.requirePriorIdleMs = object.requirePriorIdleMs ?? 0;
    message.slowReleaseOverride = object.slowReleaseOverride ?? 0;
    return message;
  },
};

function createBaseSetComboNameRequest(): SetComboNameRequest {
  return { index: 0, name: "", persist: false };
}

export const SetComboNameRequest: MessageFns<SetComboNameRequest> = {
  encode(message: SetComboNameRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.persist !== false) {
      writer.uint32(24).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetComboNameRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetComboNameRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetComboNameRequest>): SetComboNameRequest {
    return SetComboNameRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetComboNameRequest>): SetComboNameRequest {
    const message = createBaseSetComboNameRequest();
    message.index = object.index ?? 0;
    message.name = object.name ?? "";
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseDeleteComboRequest(): DeleteComboRequest {
  return { index: 0, persist: false };
}

export const DeleteComboRequest: MessageFns<DeleteComboRequest> = {
  encode(message: DeleteComboRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteComboRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteComboRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DeleteComboRequest>): DeleteComboRequest {
    return DeleteComboRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteComboRequest>): DeleteComboRequest {
    const message = createBaseDeleteComboRequest();
    message.index = object.index ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseResetComboRequest(): ResetComboRequest {
  return { index: 0 };
}

export const ResetComboRequest: MessageFns<ResetComboRequest> = {
  encode(message: ResetComboRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResetComboRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetComboRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ResetComboRequest>): ResetComboRequest {
    return ResetComboRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResetComboRequest>): ResetComboRequest {
    const message = createBaseResetComboRequest();
    message.index = object.index ?? 0;
    return message;
  },
};

function createBaseGetGlobalSettingsRequest(): GetGlobalSettingsRequest {
  return {};
}

export const GetGlobalSettingsRequest: MessageFns<GetGlobalSettingsRequest> = {
  encode(_: GetGlobalSettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGlobalSettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGlobalSettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetGlobalSettingsRequest>): GetGlobalSettingsRequest {
    return GetGlobalSettingsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetGlobalSettingsRequest>): GetGlobalSettingsRequest {
    const message = createBaseGetGlobalSettingsRequest();
    return message;
  },
};

function createBaseSetTimeoutMsRequest(): SetTimeoutMsRequest {
  return { timeoutMs: 0, persist: false };
}

export const SetTimeoutMsRequest: MessageFns<SetTimeoutMsRequest> = {
  encode(message: SetTimeoutMsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeoutMs !== 0) {
      writer.uint32(8).uint32(message.timeoutMs);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetTimeoutMsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetTimeoutMsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetTimeoutMsRequest>): SetTimeoutMsRequest {
    return SetTimeoutMsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetTimeoutMsRequest>): SetTimeoutMsRequest {
    const message = createBaseSetTimeoutMsRequest();
    message.timeoutMs = object.timeoutMs ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSetSlowReleaseRequest(): SetSlowReleaseRequest {
  return { slowRelease: false, persist: false };
}

export const SetSlowReleaseRequest: MessageFns<SetSlowReleaseRequest> = {
  encode(message: SetSlowReleaseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slowRelease !== false) {
      writer.uint32(8).bool(message.slowRelease);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetSlowReleaseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetSlowReleaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slowRelease = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetSlowReleaseRequest>): SetSlowReleaseRequest {
    return SetSlowReleaseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetSlowReleaseRequest>): SetSlowReleaseRequest {
    const message = createBaseSetSlowReleaseRequest();
    message.slowRelease = object.slowRelease ?? false;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSetRequirePriorIdleMsRequest(): SetRequirePriorIdleMsRequest {
  return { requirePriorIdleMs: 0, persist: false };
}

export const SetRequirePriorIdleMsRequest: MessageFns<SetRequirePriorIdleMsRequest> = {
  encode(message: SetRequirePriorIdleMsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requirePriorIdleMs !== 0) {
      writer.uint32(8).uint32(message.requirePriorIdleMs);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetRequirePriorIdleMsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetRequirePriorIdleMsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requirePriorIdleMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetRequirePriorIdleMsRequest>): SetRequirePriorIdleMsRequest {
    return SetRequirePriorIdleMsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetRequirePriorIdleMsRequest>): SetRequirePriorIdleMsRequest {
    const message = createBaseSetRequirePriorIdleMsRequest();
    message.requirePriorIdleMs = object.requirePriorIdleMs ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSaveRequest(): SaveRequest {
  return {};
}

export const SaveRequest: MessageFns<SaveRequest> = {
  encode(_: SaveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SaveRequest>): SaveRequest {
    return SaveRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SaveRequest>): SaveRequest {
    const message = createBaseSaveRequest();
    return message;
  },
};

function createBaseDiscardRequest(): DiscardRequest {
  return {};
}

export const DiscardRequest: MessageFns<DiscardRequest> = {
  encode(_: DiscardRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscardRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscardRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DiscardRequest>): DiscardRequest {
    return DiscardRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DiscardRequest>): DiscardRequest {
    const message = createBaseDiscardRequest();
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    listCombos: undefined,
    getCombo: undefined,
    setCombo: undefined,
    setComboName: undefined,
    deleteCombo: undefined,
    getGlobalSettings: undefined,
    setTimeoutMs: undefined,
    setSlowRelease: undefined,
    save: undefined,
    discard: undefined,
    setRequirePriorIdleMs: undefined,
    resetCombo: undefined,
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.listCombos !== undefined) {
      ListCombosRequest.encode(message.listCombos, writer.uint32(10).fork()).join();
    }
    if (message.getCombo !== undefined) {
      GetComboRequest.encode(message.getCombo, writer.uint32(18).fork()).join();
    }
    if (message.setCombo !== undefined) {
      SetComboRequest.encode(message.setCombo, writer.uint32(26).fork()).join();
    }
    if (message.setComboName !== undefined) {
      SetComboNameRequest.encode(message.setComboName, writer.uint32(34).fork()).join();
    }
    if (message.deleteCombo !== undefined) {
      DeleteComboRequest.encode(message.deleteCombo, writer.uint32(42).fork()).join();
    }
    if (message.getGlobalSettings !== undefined) {
      GetGlobalSettingsRequest.encode(message.getGlobalSettings, writer.uint32(50).fork()).join();
    }
    if (message.setTimeoutMs !== undefined) {
      SetTimeoutMsRequest.encode(message.setTimeoutMs, writer.uint32(58).fork()).join();
    }
    if (message.setSlowRelease !== undefined) {
      SetSlowReleaseRequest.encode(message.setSlowRelease, writer.uint32(66).fork()).join();
    }
    if (message.save !== undefined) {
      SaveRequest.encode(message.save, writer.uint32(74).fork()).join();
    }
    if (message.discard !== undefined) {
      DiscardRequest.encode(message.discard, writer.uint32(82).fork()).join();
    }
    if (message.setRequirePriorIdleMs !== undefined) {
      SetRequirePriorIdleMsRequest.encode(message.setRequirePriorIdleMs, writer.uint32(90).fork()).join();
    }
    if (message.resetCombo !== undefined) {
      ResetComboRequest.encode(message.resetCombo, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.listCombos = ListCombosRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.getCombo = GetComboRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.setCombo = SetComboRequest.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.setComboName = SetComboNameRequest.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deleteCombo = DeleteComboRequest.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.getGlobalSettings = GetGlobalSettingsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.setTimeoutMs = SetTimeoutMsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.setSlowRelease = SetSlowReleaseRequest.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.save = SaveRequest.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.discard = DiscardRequest.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.setRequirePriorIdleMs = SetRequirePriorIdleMsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.resetCombo = ResetComboRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.listCombos = (object.listCombos !== undefined && object.listCombos !== null)
      ? ListCombosRequest.fromPartial(object.listCombos)
      : undefined;
    message.getCombo = (object.getCombo !== undefined && object.getCombo !== null)
      ? GetComboRequest.fromPartial(object.getCombo)
      : undefined;
    message.setCombo = (object.setCombo !== undefined && object.setCombo !== null)
      ? SetComboRequest.fromPartial(object.setCombo)
      : undefined;
    message.setComboName = (object.setComboName !== undefined && object.setComboName !== null)
      ? SetComboNameRequest.fromPartial(object.setComboName)
      : undefined;
    message.deleteCombo = (object.deleteCombo !== undefined && object.deleteCombo !== null)
      ? DeleteComboRequest.fromPartial(object.deleteCombo)
      : undefined;
    message.getGlobalSettings = (object.getGlobalSettings !== undefined && object.getGlobalSettings !== null)
      ? GetGlobalSettingsRequest.fromPartial(object.getGlobalSettings)
      : undefined;
    message.setTimeoutMs = (object.setTimeoutMs !== undefined && object.setTimeoutMs !== null)
      ? SetTimeoutMsRequest.fromPartial(object.setTimeoutMs)
      : undefined;
    message.setSlowRelease = (object.setSlowRelease !== undefined && object.setSlowRelease !== null)
      ? SetSlowReleaseRequest.fromPartial(object.setSlowRelease)
      : undefined;
    message.save = (object.save !== undefined && object.save !== null)
      ? SaveRequest.fromPartial(object.save)
      : undefined;
    message.discard = (object.discard !== undefined && object.discard !== null)
      ? DiscardRequest.fromPartial(object.discard)
      : undefined;
    message.setRequirePriorIdleMs =
      (object.setRequirePriorIdleMs !== undefined && object.setRequirePriorIdleMs !== null)
        ? SetRequirePriorIdleMsRequest.fromPartial(object.setRequirePriorIdleMs)
        : undefined;
    message.resetCombo = (object.resetCombo !== undefined && object.resetCombo !== null)
      ? ResetComboRequest.fromPartial(object.resetCombo)
      : undefined;
    return message;
  },
};

function createBaseListCombosResponse(): ListCombosResponse {
  return { combos: [] };
}

export const ListCombosResponse: MessageFns<ListCombosResponse> = {
  encode(message: ListCombosResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.combos) {
      Combo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCombosResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCombosResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.combos.push(Combo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListCombosResponse>): ListCombosResponse {
    return ListCombosResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListCombosResponse>): ListCombosResponse {
    const message = createBaseListCombosResponse();
    message.combos = object.combos?.map((e) => Combo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetComboResponse(): GetComboResponse {
  return { combo: undefined };
}

export const GetComboResponse: MessageFns<GetComboResponse> = {
  encode(message: GetComboResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.combo !== undefined) {
      Combo.encode(message.combo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetComboResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetComboResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.combo = Combo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetComboResponse>): GetComboResponse {
    return GetComboResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetComboResponse>): GetComboResponse {
    const message = createBaseGetComboResponse();
    message.combo = (object.combo !== undefined && object.combo !== null) ? Combo.fromPartial(object.combo) : undefined;
    return message;
  },
};

function createBaseGetGlobalSettingsResponse(): GetGlobalSettingsResponse {
  return { settings: undefined };
}

export const GetGlobalSettingsResponse: MessageFns<GetGlobalSettingsResponse> = {
  encode(message: GetGlobalSettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settings !== undefined) {
      GlobalSettings.encode(message.settings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGlobalSettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGlobalSettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.settings = GlobalSettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetGlobalSettingsResponse>): GetGlobalSettingsResponse {
    return GetGlobalSettingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetGlobalSettingsResponse>): GetGlobalSettingsResponse {
    const message = createBaseGetGlobalSettingsResponse();
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? GlobalSettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseStatusResponse(): StatusResponse {
  return { affectedCount: 0, message: "" };
}

export const StatusResponse: MessageFns<StatusResponse> = {
  encode(message: StatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.affectedCount !== 0) {
      writer.uint32(8).uint32(message.affectedCount);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.affectedCount = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StatusResponse>): StatusResponse {
    return StatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusResponse>): StatusResponse {
    const message = createBaseStatusResponse();
    message.affectedCount = object.affectedCount ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    error: undefined,
    listCombos: undefined,
    getCombo: undefined,
    status: undefined,
    getGlobalSettings: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.listCombos !== undefined) {
      ListCombosResponse.encode(message.listCombos, writer.uint32(18).fork()).join();
    }
    if (message.getCombo !== undefined) {
      GetComboResponse.encode(message.getCombo, writer.uint32(26).fork()).join();
    }
    if (message.status !== undefined) {
      StatusResponse.encode(message.status, writer.uint32(34).fork()).join();
    }
    if (message.getGlobalSettings !== undefined) {
      GetGlobalSettingsResponse.encode(message.getGlobalSettings, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.listCombos = ListCombosResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.getCombo = GetComboResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = StatusResponse.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getGlobalSettings = GetGlobalSettingsResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.listCombos = (object.listCombos !== undefined && object.listCombos !== null)
      ? ListCombosResponse.fromPartial(object.listCombos)
      : undefined;
    message.getCombo = (object.getCombo !== undefined && object.getCombo !== null)
      ? GetComboResponse.fromPartial(object.getCombo)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? StatusResponse.fromPartial(object.status)
      : undefined;
    message.getGlobalSettings = (object.getGlobalSettings !== undefined && object.getGlobalSettings !== null)
      ? GetGlobalSettingsResponse.fromPartial(object.getGlobalSettings)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
