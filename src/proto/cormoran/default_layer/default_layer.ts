// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/default_layer/default_layer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.default_layer";

/**
 * `value` fields use a shared sentinel encoding (also used by custom
 * settings): >=0 is a keymap layer index, -1 means unset (module leaves this
 * endpoint/OS alone), -2 (endpoint values only) means "resolve via the
 * per-OS mapping using the OS detected for this endpoint".
 */
export interface GetStateRequest {
}

export interface SetEndpointLayerRequest {
  endpointIndex: number;
  value: number;
}

/**
 * os: enum zmk_os from zmk-feature-os-detection (0=unknown, 1=windows,
 * 2=macos, 3=linux, 4=ios, 5=android). value must not be -2 (OS detection is
 * not a valid value inside the per-OS mapping itself).
 */
export interface SetOsLayerRequest {
  os: number;
  value: number;
}

export interface EndpointState {
  index: number;
  isUsb: boolean;
  bleProfileIndex: number;
  value: number;
}

export interface OsLayerState {
  os: number;
  value: number;
}

export interface StateResponse {
  endpoints: EndpointState[];
  osLayers: OsLayerState[];
  activeEndpointIndex: number;
  currentOs: number;
  resolvedLayer: number;
  layerCount: number;
  osDetectionAvailable: boolean;
}

export interface ErrorResponse {
  message: string;
}

export interface Request {
  getState?: GetStateRequest | undefined;
  setEndpointLayer?: SetEndpointLayerRequest | undefined;
  setOsLayer?: SetOsLayerRequest | undefined;
}

export interface Response {
  error?: ErrorResponse | undefined;
  state?: StateResponse | undefined;
}

function createBaseGetStateRequest(): GetStateRequest {
  return {};
}

export const GetStateRequest: MessageFns<GetStateRequest> = {
  encode(_: GetStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetStateRequest>): GetStateRequest {
    return GetStateRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetStateRequest>): GetStateRequest {
    const message = createBaseGetStateRequest();
    return message;
  },
};

function createBaseSetEndpointLayerRequest(): SetEndpointLayerRequest {
  return { endpointIndex: 0, value: 0 };
}

export const SetEndpointLayerRequest: MessageFns<SetEndpointLayerRequest> = {
  encode(message: SetEndpointLayerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpointIndex !== 0) {
      writer.uint32(8).uint32(message.endpointIndex);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetEndpointLayerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetEndpointLayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.endpointIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetEndpointLayerRequest>): SetEndpointLayerRequest {
    return SetEndpointLayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetEndpointLayerRequest>): SetEndpointLayerRequest {
    const message = createBaseSetEndpointLayerRequest();
    message.endpointIndex = object.endpointIndex ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseSetOsLayerRequest(): SetOsLayerRequest {
  return { os: 0, value: 0 };
}

export const SetOsLayerRequest: MessageFns<SetOsLayerRequest> = {
  encode(message: SetOsLayerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.os !== 0) {
      writer.uint32(8).uint32(message.os);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetOsLayerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetOsLayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.os = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetOsLayerRequest>): SetOsLayerRequest {
    return SetOsLayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetOsLayerRequest>): SetOsLayerRequest {
    const message = createBaseSetOsLayerRequest();
    message.os = object.os ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseEndpointState(): EndpointState {
  return { index: 0, isUsb: false, bleProfileIndex: 0, value: 0 };
}

export const EndpointState: MessageFns<EndpointState> = {
  encode(message: EndpointState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.isUsb !== false) {
      writer.uint32(16).bool(message.isUsb);
    }
    if (message.bleProfileIndex !== 0) {
      writer.uint32(24).uint32(message.bleProfileIndex);
    }
    if (message.value !== 0) {
      writer.uint32(32).int32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndpointState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndpointState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isUsb = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.bleProfileIndex = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.value = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<EndpointState>): EndpointState {
    return EndpointState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EndpointState>): EndpointState {
    const message = createBaseEndpointState();
    message.index = object.index ?? 0;
    message.isUsb = object.isUsb ?? false;
    message.bleProfileIndex = object.bleProfileIndex ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseOsLayerState(): OsLayerState {
  return { os: 0, value: 0 };
}

export const OsLayerState: MessageFns<OsLayerState> = {
  encode(message: OsLayerState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.os !== 0) {
      writer.uint32(8).uint32(message.os);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OsLayerState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOsLayerState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.os = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<OsLayerState>): OsLayerState {
    return OsLayerState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OsLayerState>): OsLayerState {
    const message = createBaseOsLayerState();
    message.os = object.os ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseStateResponse(): StateResponse {
  return {
    endpoints: [],
    osLayers: [],
    activeEndpointIndex: 0,
    currentOs: 0,
    resolvedLayer: 0,
    layerCount: 0,
    osDetectionAvailable: false,
  };
}

export const StateResponse: MessageFns<StateResponse> = {
  encode(message: StateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.endpoints) {
      EndpointState.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.osLayers) {
      OsLayerState.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.activeEndpointIndex !== 0) {
      writer.uint32(24).uint32(message.activeEndpointIndex);
    }
    if (message.currentOs !== 0) {
      writer.uint32(32).uint32(message.currentOs);
    }
    if (message.resolvedLayer !== 0) {
      writer.uint32(40).int32(message.resolvedLayer);
    }
    if (message.layerCount !== 0) {
      writer.uint32(48).uint32(message.layerCount);
    }
    if (message.osDetectionAvailable !== false) {
      writer.uint32(56).bool(message.osDetectionAvailable);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpoints.push(EndpointState.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.osLayers.push(OsLayerState.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.activeEndpointIndex = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.currentOs = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.resolvedLayer = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.layerCount = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.osDetectionAvailable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StateResponse>): StateResponse {
    return StateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StateResponse>): StateResponse {
    const message = createBaseStateResponse();
    message.endpoints = object.endpoints?.map((e) => EndpointState.fromPartial(e)) || [];
    message.osLayers = object.osLayers?.map((e) => OsLayerState.fromPartial(e)) || [];
    message.activeEndpointIndex = object.activeEndpointIndex ?? 0;
    message.currentOs = object.currentOs ?? 0;
    message.resolvedLayer = object.resolvedLayer ?? 0;
    message.layerCount = object.layerCount ?? 0;
    message.osDetectionAvailable = object.osDetectionAvailable ?? false;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRequest(): Request {
  return { getState: undefined, setEndpointLayer: undefined, setOsLayer: undefined };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.getState !== undefined) {
      GetStateRequest.encode(message.getState, writer.uint32(10).fork()).join();
    }
    if (message.setEndpointLayer !== undefined) {
      SetEndpointLayerRequest.encode(message.setEndpointLayer, writer.uint32(18).fork()).join();
    }
    if (message.setOsLayer !== undefined) {
      SetOsLayerRequest.encode(message.setOsLayer, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.getState = GetStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setEndpointLayer = SetEndpointLayerRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.setOsLayer = SetOsLayerRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.getState = (object.getState !== undefined && object.getState !== null)
      ? GetStateRequest.fromPartial(object.getState)
      : undefined;
    message.setEndpointLayer = (object.setEndpointLayer !== undefined && object.setEndpointLayer !== null)
      ? SetEndpointLayerRequest.fromPartial(object.setEndpointLayer)
      : undefined;
    message.setOsLayer = (object.setOsLayer !== undefined && object.setOsLayer !== null)
      ? SetOsLayerRequest.fromPartial(object.setOsLayer)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return { error: undefined, state: undefined };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.state !== undefined) {
      StateResponse.encode(message.state, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = StateResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? StateResponse.fromPartial(object.state)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
