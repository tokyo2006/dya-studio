// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/os_detection/os_detection.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.os_detection";

export const Os = {
  OS_UNSPECIFIED: 0,
  OS_UNKNOWN: 1,
  OS_WINDOWS: 2,
  OS_MACOS: 3,
  OS_LINUX: 4,
  OS_IOS: 5,
  OS_ANDROID: 6,
  UNRECOGNIZED: -1,
} as const;

export type Os = typeof Os[keyof typeof Os];

export namespace Os {
  export type OS_UNSPECIFIED = typeof Os.OS_UNSPECIFIED;
  export type OS_UNKNOWN = typeof Os.OS_UNKNOWN;
  export type OS_WINDOWS = typeof Os.OS_WINDOWS;
  export type OS_MACOS = typeof Os.OS_MACOS;
  export type OS_LINUX = typeof Os.OS_LINUX;
  export type OS_IOS = typeof Os.OS_IOS;
  export type OS_ANDROID = typeof Os.OS_ANDROID;
  export type UNRECOGNIZED = typeof Os.UNRECOGNIZED;
}

export interface GetStateRequest {
}

export interface SetBleOverrideRequest {
  profileIndex: number;
  /** OS_UNSPECIFIED or OS_UNKNOWN resets to AUTO */
  os: Os;
}

export interface UsbState {
  connected: boolean;
  detected: Os;
}

export interface BleProfileState {
  index: number;
  bonded: boolean;
  connected: boolean;
  detected: Os;
  override: Os;
  effective: Os;
}

export interface StateResponse {
  usb: UsbState | undefined;
  bleProfiles: BleProfileState[];
  activeProfileIndex: number;
  currentEffective: Os;
}

export interface SetBleOverrideResponse {
  profile: BleProfileState | undefined;
}

export interface ErrorResponse {
  message: string;
}

export interface Request {
  getState?: GetStateRequest | undefined;
  setBleOverride?: SetBleOverrideRequest | undefined;
}

export interface Response {
  error?: ErrorResponse | undefined;
  state?: StateResponse | undefined;
  setBleOverride?: SetBleOverrideResponse | undefined;
}

function createBaseGetStateRequest(): GetStateRequest {
  return {};
}

export const GetStateRequest: MessageFns<GetStateRequest> = {
  encode(_: GetStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetStateRequest>): GetStateRequest {
    return GetStateRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetStateRequest>): GetStateRequest {
    const message = createBaseGetStateRequest();
    return message;
  },
};

function createBaseSetBleOverrideRequest(): SetBleOverrideRequest {
  return { profileIndex: 0, os: 0 };
}

export const SetBleOverrideRequest: MessageFns<SetBleOverrideRequest> = {
  encode(message: SetBleOverrideRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profileIndex !== 0) {
      writer.uint32(8).uint32(message.profileIndex);
    }
    if (message.os !== 0) {
      writer.uint32(16).int32(message.os);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetBleOverrideRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetBleOverrideRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.profileIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.os = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetBleOverrideRequest>): SetBleOverrideRequest {
    return SetBleOverrideRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetBleOverrideRequest>): SetBleOverrideRequest {
    const message = createBaseSetBleOverrideRequest();
    message.profileIndex = object.profileIndex ?? 0;
    message.os = object.os ?? 0;
    return message;
  },
};

function createBaseUsbState(): UsbState {
  return { connected: false, detected: 0 };
}

export const UsbState: MessageFns<UsbState> = {
  encode(message: UsbState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.connected !== false) {
      writer.uint32(8).bool(message.connected);
    }
    if (message.detected !== 0) {
      writer.uint32(16).int32(message.detected);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UsbState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsbState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.connected = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.detected = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<UsbState>): UsbState {
    return UsbState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UsbState>): UsbState {
    const message = createBaseUsbState();
    message.connected = object.connected ?? false;
    message.detected = object.detected ?? 0;
    return message;
  },
};

function createBaseBleProfileState(): BleProfileState {
  return { index: 0, bonded: false, connected: false, detected: 0, override: 0, effective: 0 };
}

export const BleProfileState: MessageFns<BleProfileState> = {
  encode(message: BleProfileState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.bonded !== false) {
      writer.uint32(16).bool(message.bonded);
    }
    if (message.connected !== false) {
      writer.uint32(24).bool(message.connected);
    }
    if (message.detected !== 0) {
      writer.uint32(32).int32(message.detected);
    }
    if (message.override !== 0) {
      writer.uint32(40).int32(message.override);
    }
    if (message.effective !== 0) {
      writer.uint32(48).int32(message.effective);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BleProfileState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBleProfileState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.bonded = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.connected = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.detected = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.override = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.effective = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<BleProfileState>): BleProfileState {
    return BleProfileState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BleProfileState>): BleProfileState {
    const message = createBaseBleProfileState();
    message.index = object.index ?? 0;
    message.bonded = object.bonded ?? false;
    message.connected = object.connected ?? false;
    message.detected = object.detected ?? 0;
    message.override = object.override ?? 0;
    message.effective = object.effective ?? 0;
    return message;
  },
};

function createBaseStateResponse(): StateResponse {
  return { usb: undefined, bleProfiles: [], activeProfileIndex: 0, currentEffective: 0 };
}

export const StateResponse: MessageFns<StateResponse> = {
  encode(message: StateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.usb !== undefined) {
      UsbState.encode(message.usb, writer.uint32(10).fork()).join();
    }
    for (const v of message.bleProfiles) {
      BleProfileState.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.activeProfileIndex !== 0) {
      writer.uint32(24).uint32(message.activeProfileIndex);
    }
    if (message.currentEffective !== 0) {
      writer.uint32(32).int32(message.currentEffective);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.usb = UsbState.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bleProfiles.push(BleProfileState.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.activeProfileIndex = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.currentEffective = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StateResponse>): StateResponse {
    return StateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StateResponse>): StateResponse {
    const message = createBaseStateResponse();
    message.usb = (object.usb !== undefined && object.usb !== null) ? UsbState.fromPartial(object.usb) : undefined;
    message.bleProfiles = object.bleProfiles?.map((e) => BleProfileState.fromPartial(e)) || [];
    message.activeProfileIndex = object.activeProfileIndex ?? 0;
    message.currentEffective = object.currentEffective ?? 0;
    return message;
  },
};

function createBaseSetBleOverrideResponse(): SetBleOverrideResponse {
  return { profile: undefined };
}

export const SetBleOverrideResponse: MessageFns<SetBleOverrideResponse> = {
  encode(message: SetBleOverrideResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      BleProfileState.encode(message.profile, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetBleOverrideResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetBleOverrideResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = BleProfileState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetBleOverrideResponse>): SetBleOverrideResponse {
    return SetBleOverrideResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetBleOverrideResponse>): SetBleOverrideResponse {
    const message = createBaseSetBleOverrideResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? BleProfileState.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRequest(): Request {
  return { getState: undefined, setBleOverride: undefined };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.getState !== undefined) {
      GetStateRequest.encode(message.getState, writer.uint32(10).fork()).join();
    }
    if (message.setBleOverride !== undefined) {
      SetBleOverrideRequest.encode(message.setBleOverride, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.getState = GetStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setBleOverride = SetBleOverrideRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.getState = (object.getState !== undefined && object.getState !== null)
      ? GetStateRequest.fromPartial(object.getState)
      : undefined;
    message.setBleOverride = (object.setBleOverride !== undefined && object.setBleOverride !== null)
      ? SetBleOverrideRequest.fromPartial(object.setBleOverride)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return { error: undefined, state: undefined, setBleOverride: undefined };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.state !== undefined) {
      StateResponse.encode(message.state, writer.uint32(18).fork()).join();
    }
    if (message.setBleOverride !== undefined) {
      SetBleOverrideResponse.encode(message.setBleOverride, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = StateResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.setBleOverride = SetBleOverrideResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? StateResponse.fromPartial(object.state)
      : undefined;
    message.setBleOverride = (object.setBleOverride !== undefined && object.setBleOverride !== null)
      ? SetBleOverrideResponse.fromPartial(object.setBleOverride)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
