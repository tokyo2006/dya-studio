// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/kscan_diagnostics/kscan_diagnostics.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.kscan_diagnostics";

export const KscanDriverType = {
  UNKNOWN: 0,
  MATRIX: 1,
  DIRECT: 2,
  CHARLIEPLEX: 3,
  DEMUX: 4,
  MOCK: 5,
  UNRECOGNIZED: -1,
} as const;

export type KscanDriverType = typeof KscanDriverType[keyof typeof KscanDriverType];

export namespace KscanDriverType {
  export type UNKNOWN = typeof KscanDriverType.UNKNOWN;
  export type MATRIX = typeof KscanDriverType.MATRIX;
  export type DIRECT = typeof KscanDriverType.DIRECT;
  export type CHARLIEPLEX = typeof KscanDriverType.CHARLIEPLEX;
  export type DEMUX = typeof KscanDriverType.DEMUX;
  export type MOCK = typeof KscanDriverType.MOCK;
  export type UNRECOGNIZED = typeof KscanDriverType.UNRECOGNIZED;
}

export const GpioLineKind = {
  KIND_UNKNOWN: 0,
  ROW: 1,
  COL: 2,
  INPUT: 3,
  OUTPUT: 4,
  CHARLIE: 5,
  UNRECOGNIZED: -1,
} as const;

export type GpioLineKind = typeof GpioLineKind[keyof typeof GpioLineKind];

export namespace GpioLineKind {
  export type KIND_UNKNOWN = typeof GpioLineKind.KIND_UNKNOWN;
  export type ROW = typeof GpioLineKind.ROW;
  export type COL = typeof GpioLineKind.COL;
  export type INPUT = typeof GpioLineKind.INPUT;
  export type OUTPUT = typeof GpioLineKind.OUTPUT;
  export type CHARLIE = typeof GpioLineKind.CHARLIE;
  export type UNRECOGNIZED = typeof GpioLineKind.UNRECOGNIZED;
}

export interface GetInfo {
}

export interface Info {
  protoVersion: number;
  layoutCount: number;
  selectedLayout: number;
  deviceCount: number;
  statsEnabled: boolean;
  maxPositions: number;
  uptimeMs: number;
}

export interface GetLayout {
  layoutIndex: number;
}

export interface LayoutDevice {
  leafIndex: number;
  rowOffset: number;
  colOffset: number;
}

export interface Layout {
  layoutIndex: number;
  displayName: string;
  rows: number;
  columns: number;
  keyCount: number;
  deviceIndices: LayoutDevice[];
}

export interface GetDevice {
  deviceIndex: number;
}

export interface Device {
  deviceIndex: number;
  nodeName: string;
  type: KscanDriverType;
  rows: number;
  columns: number;
  inputs: number;
  debouncePressMs: number;
  debounceReleaseMs: number;
  debounceScanPeriodMs: number;
  pollPeriodMs: number;
  diodeRow2col: boolean;
  toggleMode: boolean;
}

export interface GetGpioPins {
  deviceIndex: number;
  /**
   * KIND_UNKNOWN means unfiltered: return every GPIO line regardless of
   * kind. Any other value filters to lines of exactly that kind.
   */
  kind: GpioLineKind;
  offset: number;
}

export interface GpioPin {
  index: number;
  port: string;
  pin: number;
  activeLow: boolean;
  dtFlags: number;
}

export interface GpioPins {
  total: number;
  offset: number;
  pins: GpioPin[];
}

export interface GetPositionMap {
  layoutIndex: number;
  offset: number;
}

export interface PositionMap {
  total: number;
  offset: number;
  cells: number[];
}

export interface Error {
  message: string;
}

export interface GetStats {
  offset: number;
}

export interface PositionStats {
  position: number;
  presses: number;
  releases: number;
  minPressDurationMs: number;
  minRepressGapMs: number;
  repressLt5: number;
  repressLt10: number;
  repressLt20: number;
  repressLt50: number;
  lastSource: number;
}

export interface Stats {
  total: number;
  offset: number;
  entries: PositionStats[];
}

export interface ResetStats {
}

export interface Ok {
}

export interface Request {
  getInfo?: GetInfo | undefined;
  getLayout?: GetLayout | undefined;
  getDevice?: GetDevice | undefined;
  getGpioPins?: GetGpioPins | undefined;
  getPositionMap?: GetPositionMap | undefined;
  getStats?: GetStats | undefined;
  resetStats?: ResetStats | undefined;
}

export interface Response {
  error?: Error | undefined;
  info?: Info | undefined;
  layout?: Layout | undefined;
  device?: Device | undefined;
  gpioPins?: GpioPins | undefined;
  positionMap?: PositionMap | undefined;
  stats?: Stats | undefined;
  ok?: Ok | undefined;
}

function createBaseGetInfo(): GetInfo {
  return {};
}

export const GetInfo: MessageFns<GetInfo> = {
  encode(_: GetInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetInfo>): GetInfo {
    return GetInfo.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetInfo>): GetInfo {
    const message = createBaseGetInfo();
    return message;
  },
};

function createBaseInfo(): Info {
  return {
    protoVersion: 0,
    layoutCount: 0,
    selectedLayout: 0,
    deviceCount: 0,
    statsEnabled: false,
    maxPositions: 0,
    uptimeMs: 0,
  };
}

export const Info: MessageFns<Info> = {
  encode(message: Info, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protoVersion !== 0) {
      writer.uint32(8).uint32(message.protoVersion);
    }
    if (message.layoutCount !== 0) {
      writer.uint32(16).uint32(message.layoutCount);
    }
    if (message.selectedLayout !== 0) {
      writer.uint32(24).uint32(message.selectedLayout);
    }
    if (message.deviceCount !== 0) {
      writer.uint32(32).uint32(message.deviceCount);
    }
    if (message.statsEnabled !== false) {
      writer.uint32(40).bool(message.statsEnabled);
    }
    if (message.maxPositions !== 0) {
      writer.uint32(48).uint32(message.maxPositions);
    }
    if (message.uptimeMs !== 0) {
      writer.uint32(56).uint32(message.uptimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Info {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protoVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.layoutCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.selectedLayout = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deviceCount = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.statsEnabled = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxPositions = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.uptimeMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Info>): Info {
    return Info.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Info>): Info {
    const message = createBaseInfo();
    message.protoVersion = object.protoVersion ?? 0;
    message.layoutCount = object.layoutCount ?? 0;
    message.selectedLayout = object.selectedLayout ?? 0;
    message.deviceCount = object.deviceCount ?? 0;
    message.statsEnabled = object.statsEnabled ?? false;
    message.maxPositions = object.maxPositions ?? 0;
    message.uptimeMs = object.uptimeMs ?? 0;
    return message;
  },
};

function createBaseGetLayout(): GetLayout {
  return { layoutIndex: 0 };
}

export const GetLayout: MessageFns<GetLayout> = {
  encode(message: GetLayout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layoutIndex !== 0) {
      writer.uint32(8).uint32(message.layoutIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLayout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layoutIndex = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetLayout>): GetLayout {
    return GetLayout.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLayout>): GetLayout {
    const message = createBaseGetLayout();
    message.layoutIndex = object.layoutIndex ?? 0;
    return message;
  },
};

function createBaseLayoutDevice(): LayoutDevice {
  return { leafIndex: 0, rowOffset: 0, colOffset: 0 };
}

export const LayoutDevice: MessageFns<LayoutDevice> = {
  encode(message: LayoutDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leafIndex !== 0) {
      writer.uint32(8).uint32(message.leafIndex);
    }
    if (message.rowOffset !== 0) {
      writer.uint32(16).int32(message.rowOffset);
    }
    if (message.colOffset !== 0) {
      writer.uint32(24).int32(message.colOffset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LayoutDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayoutDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.leafIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rowOffset = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.colOffset = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<LayoutDevice>): LayoutDevice {
    return LayoutDevice.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LayoutDevice>): LayoutDevice {
    const message = createBaseLayoutDevice();
    message.leafIndex = object.leafIndex ?? 0;
    message.rowOffset = object.rowOffset ?? 0;
    message.colOffset = object.colOffset ?? 0;
    return message;
  },
};

function createBaseLayout(): Layout {
  return { layoutIndex: 0, displayName: "", rows: 0, columns: 0, keyCount: 0, deviceIndices: [] };
}

export const Layout: MessageFns<Layout> = {
  encode(message: Layout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layoutIndex !== 0) {
      writer.uint32(8).uint32(message.layoutIndex);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.rows !== 0) {
      writer.uint32(24).uint32(message.rows);
    }
    if (message.columns !== 0) {
      writer.uint32(32).uint32(message.columns);
    }
    if (message.keyCount !== 0) {
      writer.uint32(40).uint32(message.keyCount);
    }
    for (const v of message.deviceIndices) {
      LayoutDevice.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Layout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layoutIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.columns = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.keyCount = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.deviceIndices.push(LayoutDevice.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Layout>): Layout {
    return Layout.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Layout>): Layout {
    const message = createBaseLayout();
    message.layoutIndex = object.layoutIndex ?? 0;
    message.displayName = object.displayName ?? "";
    message.rows = object.rows ?? 0;
    message.columns = object.columns ?? 0;
    message.keyCount = object.keyCount ?? 0;
    message.deviceIndices = object.deviceIndices?.map((e) => LayoutDevice.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetDevice(): GetDevice {
  return { deviceIndex: 0 };
}

export const GetDevice: MessageFns<GetDevice> = {
  encode(message: GetDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceIndex !== 0) {
      writer.uint32(8).uint32(message.deviceIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceIndex = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetDevice>): GetDevice {
    return GetDevice.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDevice>): GetDevice {
    const message = createBaseGetDevice();
    message.deviceIndex = object.deviceIndex ?? 0;
    return message;
  },
};

function createBaseDevice(): Device {
  return {
    deviceIndex: 0,
    nodeName: "",
    type: 0,
    rows: 0,
    columns: 0,
    inputs: 0,
    debouncePressMs: 0,
    debounceReleaseMs: 0,
    debounceScanPeriodMs: 0,
    pollPeriodMs: 0,
    diodeRow2col: false,
    toggleMode: false,
  };
}

export const Device: MessageFns<Device> = {
  encode(message: Device, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceIndex !== 0) {
      writer.uint32(8).uint32(message.deviceIndex);
    }
    if (message.nodeName !== "") {
      writer.uint32(18).string(message.nodeName);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.rows !== 0) {
      writer.uint32(32).uint32(message.rows);
    }
    if (message.columns !== 0) {
      writer.uint32(40).uint32(message.columns);
    }
    if (message.inputs !== 0) {
      writer.uint32(48).uint32(message.inputs);
    }
    if (message.debouncePressMs !== 0) {
      writer.uint32(56).uint32(message.debouncePressMs);
    }
    if (message.debounceReleaseMs !== 0) {
      writer.uint32(64).uint32(message.debounceReleaseMs);
    }
    if (message.debounceScanPeriodMs !== 0) {
      writer.uint32(72).uint32(message.debounceScanPeriodMs);
    }
    if (message.pollPeriodMs !== 0) {
      writer.uint32(80).uint32(message.pollPeriodMs);
    }
    if (message.diodeRow2col !== false) {
      writer.uint32(88).bool(message.diodeRow2col);
    }
    if (message.toggleMode !== false) {
      writer.uint32(96).bool(message.toggleMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Device {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.columns = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.inputs = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.debouncePressMs = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.debounceReleaseMs = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.debounceScanPeriodMs = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.pollPeriodMs = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.diodeRow2col = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.toggleMode = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Device>): Device {
    return Device.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Device>): Device {
    const message = createBaseDevice();
    message.deviceIndex = object.deviceIndex ?? 0;
    message.nodeName = object.nodeName ?? "";
    message.type = object.type ?? 0;
    message.rows = object.rows ?? 0;
    message.columns = object.columns ?? 0;
    message.inputs = object.inputs ?? 0;
    message.debouncePressMs = object.debouncePressMs ?? 0;
    message.debounceReleaseMs = object.debounceReleaseMs ?? 0;
    message.debounceScanPeriodMs = object.debounceScanPeriodMs ?? 0;
    message.pollPeriodMs = object.pollPeriodMs ?? 0;
    message.diodeRow2col = object.diodeRow2col ?? false;
    message.toggleMode = object.toggleMode ?? false;
    return message;
  },
};

function createBaseGetGpioPins(): GetGpioPins {
  return { deviceIndex: 0, kind: 0, offset: 0 };
}

export const GetGpioPins: MessageFns<GetGpioPins> = {
  encode(message: GetGpioPins, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceIndex !== 0) {
      writer.uint32(8).uint32(message.deviceIndex);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    if (message.offset !== 0) {
      writer.uint32(24).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetGpioPins {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetGpioPins();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetGpioPins>): GetGpioPins {
    return GetGpioPins.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetGpioPins>): GetGpioPins {
    const message = createBaseGetGpioPins();
    message.deviceIndex = object.deviceIndex ?? 0;
    message.kind = object.kind ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBaseGpioPin(): GpioPin {
  return { index: 0, port: "", pin: 0, activeLow: false, dtFlags: 0 };
}

export const GpioPin: MessageFns<GpioPin> = {
  encode(message: GpioPin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.port !== "") {
      writer.uint32(18).string(message.port);
    }
    if (message.pin !== 0) {
      writer.uint32(24).uint32(message.pin);
    }
    if (message.activeLow !== false) {
      writer.uint32(32).bool(message.activeLow);
    }
    if (message.dtFlags !== 0) {
      writer.uint32(40).uint32(message.dtFlags);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GpioPin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGpioPin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.port = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pin = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.activeLow = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.dtFlags = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GpioPin>): GpioPin {
    return GpioPin.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GpioPin>): GpioPin {
    const message = createBaseGpioPin();
    message.index = object.index ?? 0;
    message.port = object.port ?? "";
    message.pin = object.pin ?? 0;
    message.activeLow = object.activeLow ?? false;
    message.dtFlags = object.dtFlags ?? 0;
    return message;
  },
};

function createBaseGpioPins(): GpioPins {
  return { total: 0, offset: 0, pins: [] };
}

export const GpioPins: MessageFns<GpioPins> = {
  encode(message: GpioPins, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).uint32(message.total);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    for (const v of message.pins) {
      GpioPin.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GpioPins {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGpioPins();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pins.push(GpioPin.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GpioPins>): GpioPins {
    return GpioPins.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GpioPins>): GpioPins {
    const message = createBaseGpioPins();
    message.total = object.total ?? 0;
    message.offset = object.offset ?? 0;
    message.pins = object.pins?.map((e) => GpioPin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetPositionMap(): GetPositionMap {
  return { layoutIndex: 0, offset: 0 };
}

export const GetPositionMap: MessageFns<GetPositionMap> = {
  encode(message: GetPositionMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layoutIndex !== 0) {
      writer.uint32(8).uint32(message.layoutIndex);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPositionMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPositionMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layoutIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetPositionMap>): GetPositionMap {
    return GetPositionMap.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPositionMap>): GetPositionMap {
    const message = createBaseGetPositionMap();
    message.layoutIndex = object.layoutIndex ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBasePositionMap(): PositionMap {
  return { total: 0, offset: 0, cells: [] };
}

export const PositionMap: MessageFns<PositionMap> = {
  encode(message: PositionMap, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).uint32(message.total);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    writer.uint32(26).fork();
    for (const v of message.cells) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PositionMap {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePositionMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.cells.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cells.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<PositionMap>): PositionMap {
    return PositionMap.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PositionMap>): PositionMap {
    const message = createBasePositionMap();
    message.total = object.total ?? 0;
    message.offset = object.offset ?? 0;
    message.cells = object.cells?.map((e) => e) || [];
    return message;
  },
};

function createBaseError(): Error {
  return { message: "" };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Error>): Error {
    return Error.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Error>): Error {
    const message = createBaseError();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseGetStats(): GetStats {
  return { offset: 0 };
}

export const GetStats: MessageFns<GetStats> = {
  encode(message: GetStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.offset !== 0) {
      writer.uint32(8).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetStats>): GetStats {
    return GetStats.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStats>): GetStats {
    const message = createBaseGetStats();
    message.offset = object.offset ?? 0;
    return message;
  },
};

function createBasePositionStats(): PositionStats {
  return {
    position: 0,
    presses: 0,
    releases: 0,
    minPressDurationMs: 0,
    minRepressGapMs: 0,
    repressLt5: 0,
    repressLt10: 0,
    repressLt20: 0,
    repressLt50: 0,
    lastSource: 0,
  };
}

export const PositionStats: MessageFns<PositionStats> = {
  encode(message: PositionStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== 0) {
      writer.uint32(8).uint32(message.position);
    }
    if (message.presses !== 0) {
      writer.uint32(16).uint32(message.presses);
    }
    if (message.releases !== 0) {
      writer.uint32(24).uint32(message.releases);
    }
    if (message.minPressDurationMs !== 0) {
      writer.uint32(32).uint32(message.minPressDurationMs);
    }
    if (message.minRepressGapMs !== 0) {
      writer.uint32(40).uint32(message.minRepressGapMs);
    }
    if (message.repressLt5 !== 0) {
      writer.uint32(48).uint32(message.repressLt5);
    }
    if (message.repressLt10 !== 0) {
      writer.uint32(56).uint32(message.repressLt10);
    }
    if (message.repressLt20 !== 0) {
      writer.uint32(64).uint32(message.repressLt20);
    }
    if (message.repressLt50 !== 0) {
      writer.uint32(72).uint32(message.repressLt50);
    }
    if (message.lastSource !== 0) {
      writer.uint32(80).uint32(message.lastSource);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PositionStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePositionStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.position = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.presses = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.releases = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.minPressDurationMs = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.minRepressGapMs = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.repressLt5 = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.repressLt10 = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.repressLt20 = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.repressLt50 = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.lastSource = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<PositionStats>): PositionStats {
    return PositionStats.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PositionStats>): PositionStats {
    const message = createBasePositionStats();
    message.position = object.position ?? 0;
    message.presses = object.presses ?? 0;
    message.releases = object.releases ?? 0;
    message.minPressDurationMs = object.minPressDurationMs ?? 0;
    message.minRepressGapMs = object.minRepressGapMs ?? 0;
    message.repressLt5 = object.repressLt5 ?? 0;
    message.repressLt10 = object.repressLt10 ?? 0;
    message.repressLt20 = object.repressLt20 ?? 0;
    message.repressLt50 = object.repressLt50 ?? 0;
    message.lastSource = object.lastSource ?? 0;
    return message;
  },
};

function createBaseStats(): Stats {
  return { total: 0, offset: 0, entries: [] };
}

export const Stats: MessageFns<Stats> = {
  encode(message: Stats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.total !== 0) {
      writer.uint32(8).uint32(message.total);
    }
    if (message.offset !== 0) {
      writer.uint32(16).uint32(message.offset);
    }
    for (const v of message.entries) {
      PositionStats.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Stats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.total = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entries.push(PositionStats.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Stats>): Stats {
    return Stats.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Stats>): Stats {
    const message = createBaseStats();
    message.total = object.total ?? 0;
    message.offset = object.offset ?? 0;
    message.entries = object.entries?.map((e) => PositionStats.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResetStats(): ResetStats {
  return {};
}

export const ResetStats: MessageFns<ResetStats> = {
  encode(_: ResetStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResetStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ResetStats>): ResetStats {
    return ResetStats.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ResetStats>): ResetStats {
    const message = createBaseResetStats();
    return message;
  },
};

function createBaseOk(): Ok {
  return {};
}

export const Ok: MessageFns<Ok> = {
  encode(_: Ok, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ok {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Ok>): Ok {
    return Ok.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<Ok>): Ok {
    const message = createBaseOk();
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    getInfo: undefined,
    getLayout: undefined,
    getDevice: undefined,
    getGpioPins: undefined,
    getPositionMap: undefined,
    getStats: undefined,
    resetStats: undefined,
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.getInfo !== undefined) {
      GetInfo.encode(message.getInfo, writer.uint32(10).fork()).join();
    }
    if (message.getLayout !== undefined) {
      GetLayout.encode(message.getLayout, writer.uint32(18).fork()).join();
    }
    if (message.getDevice !== undefined) {
      GetDevice.encode(message.getDevice, writer.uint32(26).fork()).join();
    }
    if (message.getGpioPins !== undefined) {
      GetGpioPins.encode(message.getGpioPins, writer.uint32(34).fork()).join();
    }
    if (message.getPositionMap !== undefined) {
      GetPositionMap.encode(message.getPositionMap, writer.uint32(42).fork()).join();
    }
    if (message.getStats !== undefined) {
      GetStats.encode(message.getStats, writer.uint32(50).fork()).join();
    }
    if (message.resetStats !== undefined) {
      ResetStats.encode(message.resetStats, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.getInfo = GetInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.getLayout = GetLayout.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.getDevice = GetDevice.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.getGpioPins = GetGpioPins.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getPositionMap = GetPositionMap.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.getStats = GetStats.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.resetStats = ResetStats.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.getInfo = (object.getInfo !== undefined && object.getInfo !== null)
      ? GetInfo.fromPartial(object.getInfo)
      : undefined;
    message.getLayout = (object.getLayout !== undefined && object.getLayout !== null)
      ? GetLayout.fromPartial(object.getLayout)
      : undefined;
    message.getDevice = (object.getDevice !== undefined && object.getDevice !== null)
      ? GetDevice.fromPartial(object.getDevice)
      : undefined;
    message.getGpioPins = (object.getGpioPins !== undefined && object.getGpioPins !== null)
      ? GetGpioPins.fromPartial(object.getGpioPins)
      : undefined;
    message.getPositionMap = (object.getPositionMap !== undefined && object.getPositionMap !== null)
      ? GetPositionMap.fromPartial(object.getPositionMap)
      : undefined;
    message.getStats = (object.getStats !== undefined && object.getStats !== null)
      ? GetStats.fromPartial(object.getStats)
      : undefined;
    message.resetStats = (object.resetStats !== undefined && object.resetStats !== null)
      ? ResetStats.fromPartial(object.resetStats)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    error: undefined,
    info: undefined,
    layout: undefined,
    device: undefined,
    gpioPins: undefined,
    positionMap: undefined,
    stats: undefined,
    ok: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.info !== undefined) {
      Info.encode(message.info, writer.uint32(18).fork()).join();
    }
    if (message.layout !== undefined) {
      Layout.encode(message.layout, writer.uint32(26).fork()).join();
    }
    if (message.device !== undefined) {
      Device.encode(message.device, writer.uint32(34).fork()).join();
    }
    if (message.gpioPins !== undefined) {
      GpioPins.encode(message.gpioPins, writer.uint32(42).fork()).join();
    }
    if (message.positionMap !== undefined) {
      PositionMap.encode(message.positionMap, writer.uint32(50).fork()).join();
    }
    if (message.stats !== undefined) {
      Stats.encode(message.stats, writer.uint32(58).fork()).join();
    }
    if (message.ok !== undefined) {
      Ok.encode(message.ok, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.info = Info.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.layout = Layout.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.device = Device.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gpioPins = GpioPins.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.positionMap = PositionMap.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.stats = Stats.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.ok = Ok.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    message.info = (object.info !== undefined && object.info !== null) ? Info.fromPartial(object.info) : undefined;
    message.layout = (object.layout !== undefined && object.layout !== null)
      ? Layout.fromPartial(object.layout)
      : undefined;
    message.device = (object.device !== undefined && object.device !== null)
      ? Device.fromPartial(object.device)
      : undefined;
    message.gpioPins = (object.gpioPins !== undefined && object.gpioPins !== null)
      ? GpioPins.fromPartial(object.gpioPins)
      : undefined;
    message.positionMap = (object.positionMap !== undefined && object.positionMap !== null)
      ? PositionMap.fromPartial(object.positionMap)
      : undefined;
    message.stats = (object.stats !== undefined && object.stats !== null) ? Stats.fromPartial(object.stats) : undefined;
    message.ok = (object.ok !== undefined && object.ok !== null) ? Ok.fromPartial(object.ok) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
