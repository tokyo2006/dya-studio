// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/runtime_macro/runtime_macro.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.runtime_macro";

export interface ListMacrosRequest {
}

export interface GetMacroRequest {
  slot: number;
}

export interface GetMacroGlobalSettingsRequest {
}

export interface MacroSummary {
  slot: number;
  name: string;
  encodedSize: number;
  /**
   * True when the macro has an in-memory-only change not yet written to
   * flash (a memory-mode create/edit, or a value that differs from the
   * persisted one). Cleared by SaveMacros / DiscardMacros. A devicetree
   * default that has never been saved also reports true, since it lives
   * only in memory until the user saves it.
   */
  hasUnsavedChanges: boolean;
}

export interface BehaviorBinding {
  behaviorId: number;
  param1: number;
  param2: number;
}

export interface DelayStep {
  delayMs: number;
}

export interface KeyTapSequenceStep {
  packedKeys: Uint8Array;
}

export interface MacroStep {
  down?: BehaviorBinding | undefined;
  up?: BehaviorBinding | undefined;
  tap?: BehaviorBinding | undefined;
  delay?: DelayStep | undefined;
  keyTapSequence?: KeyTapSequenceStep | undefined;
}

export interface MacroDetail {
  slot: number;
  name: string;
  steps: MacroStep[];
  encodedSize: number;
}

export interface SetMacroStepCountRequest {
  slot: number;
  stepCount: number;
  persist: boolean;
}

export interface SetMacroStepRequest {
  slot: number;
  stepIndex: number;
  step: MacroStep | undefined;
  persist: boolean;
}

export interface AppendMacroStepRequest {
  slot: number;
  step: MacroStep | undefined;
  persist: boolean;
}

export interface SetTapMsRequest {
  tapMs: number;
  persist: boolean;
}

export interface SaveMacrosRequest {
}

export interface DiscardMacrosRequest {
}

export interface CreateMacroRequest {
  name: string;
  persist: boolean;
}

export interface DeleteMacroRequest {
  name: string;
}

export interface RenameMacroRequest {
  oldName: string;
  newName: string;
  persist: boolean;
}

/**
 * Reset the macro at `slot` to its compile-time (devicetree) state: if a
 * `cormoran,runtime-macro-default` node declares a macro whose name matches
 * this macro's, overwrite the body with that default; otherwise (no such
 * compile-time default) delete the macro entirely - "reset" of a macro that
 * only ever existed at runtime means removing it. `persist` writes the reset
 * result straight to flash (otherwise it lives in memory until SaveMacros /
 * is re-seeded from the devicetree next boot).
 */
export interface ResetMacroRequest {
  slot: number;
  persist: boolean;
}

export interface Request {
  listMacros?: ListMacrosRequest | undefined;
  getMacro?: GetMacroRequest | undefined;
  setMacroStepCount?: SetMacroStepCountRequest | undefined;
  getMacroGlobalSettings?: GetMacroGlobalSettingsRequest | undefined;
  setTapMs?: SetTapMsRequest | undefined;
  setMacroStep?: SetMacroStepRequest | undefined;
  saveMacros?: SaveMacrosRequest | undefined;
  discardMacros?: DiscardMacrosRequest | undefined;
  appendMacroStep?: AppendMacroStepRequest | undefined;
  createMacro?: CreateMacroRequest | undefined;
  deleteMacro?: DeleteMacroRequest | undefined;
  renameMacro?: RenameMacroRequest | undefined;
  resetMacro?: ResetMacroRequest | undefined;
}

export interface ErrorResponse {
  message: string;
}

export interface StatusResponse {
  affectedCount: number;
  message: string;
}

export interface ListMacrosResponse {
  macros: MacroSummary[];
  maxMacroBytes: number;
  maxNameLength: number;
}

export interface GetMacroResponse {
  macro: MacroDetail | undefined;
}

export interface MacroGlobalSettings {
  tapMs: number;
  maxEntries: number;
  keyPressBehaviorId: number;
  /**
   * Shared macro name+body pool budget (CONFIG_ZMK_RUNTIME_MACRO_POOL_BYTES)
   * and the bytes currently occupied by every macro combined.
   */
  poolBytesTotal: number;
  poolBytesUsed: number;
}

export interface GetMacroGlobalSettingsResponse {
  settings: MacroGlobalSettings | undefined;
}

export interface Response {
  error?: ErrorResponse | undefined;
  status?: StatusResponse | undefined;
  listMacros?: ListMacrosResponse | undefined;
  getMacro?: GetMacroResponse | undefined;
  getMacroGlobalSettings?: GetMacroGlobalSettingsResponse | undefined;
}

function createBaseListMacrosRequest(): ListMacrosRequest {
  return {};
}

export const ListMacrosRequest: MessageFns<ListMacrosRequest> = {
  encode(_: ListMacrosRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMacrosRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMacrosRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListMacrosRequest>): ListMacrosRequest {
    return ListMacrosRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListMacrosRequest>): ListMacrosRequest {
    const message = createBaseListMacrosRequest();
    return message;
  },
};

function createBaseGetMacroRequest(): GetMacroRequest {
  return { slot: 0 };
}

export const GetMacroRequest: MessageFns<GetMacroRequest> = {
  encode(message: GetMacroRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMacroRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMacroRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetMacroRequest>): GetMacroRequest {
    return GetMacroRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMacroRequest>): GetMacroRequest {
    const message = createBaseGetMacroRequest();
    message.slot = object.slot ?? 0;
    return message;
  },
};

function createBaseGetMacroGlobalSettingsRequest(): GetMacroGlobalSettingsRequest {
  return {};
}

export const GetMacroGlobalSettingsRequest: MessageFns<GetMacroGlobalSettingsRequest> = {
  encode(_: GetMacroGlobalSettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMacroGlobalSettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMacroGlobalSettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetMacroGlobalSettingsRequest>): GetMacroGlobalSettingsRequest {
    return GetMacroGlobalSettingsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetMacroGlobalSettingsRequest>): GetMacroGlobalSettingsRequest {
    const message = createBaseGetMacroGlobalSettingsRequest();
    return message;
  },
};

function createBaseMacroSummary(): MacroSummary {
  return { slot: 0, name: "", encodedSize: 0, hasUnsavedChanges: false };
}

export const MacroSummary: MessageFns<MacroSummary> = {
  encode(message: MacroSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.encodedSize !== 0) {
      writer.uint32(24).uint32(message.encodedSize);
    }
    if (message.hasUnsavedChanges !== false) {
      writer.uint32(32).bool(message.hasUnsavedChanges);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MacroSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMacroSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.encodedSize = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hasUnsavedChanges = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<MacroSummary>): MacroSummary {
    return MacroSummary.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MacroSummary>): MacroSummary {
    const message = createBaseMacroSummary();
    message.slot = object.slot ?? 0;
    message.name = object.name ?? "";
    message.encodedSize = object.encodedSize ?? 0;
    message.hasUnsavedChanges = object.hasUnsavedChanges ?? false;
    return message;
  },
};

function createBaseBehaviorBinding(): BehaviorBinding {
  return { behaviorId: 0, param1: 0, param2: 0 };
}

export const BehaviorBinding: MessageFns<BehaviorBinding> = {
  encode(message: BehaviorBinding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.behaviorId !== 0) {
      writer.uint32(8).uint32(message.behaviorId);
    }
    if (message.param1 !== 0) {
      writer.uint32(16).uint32(message.param1);
    }
    if (message.param2 !== 0) {
      writer.uint32(24).uint32(message.param2);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BehaviorBinding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBehaviorBinding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.behaviorId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.param1 = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.param2 = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<BehaviorBinding>): BehaviorBinding {
    return BehaviorBinding.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BehaviorBinding>): BehaviorBinding {
    const message = createBaseBehaviorBinding();
    message.behaviorId = object.behaviorId ?? 0;
    message.param1 = object.param1 ?? 0;
    message.param2 = object.param2 ?? 0;
    return message;
  },
};

function createBaseDelayStep(): DelayStep {
  return { delayMs: 0 };
}

export const DelayStep: MessageFns<DelayStep> = {
  encode(message: DelayStep, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.delayMs !== 0) {
      writer.uint32(8).uint32(message.delayMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DelayStep {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelayStep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.delayMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DelayStep>): DelayStep {
    return DelayStep.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DelayStep>): DelayStep {
    const message = createBaseDelayStep();
    message.delayMs = object.delayMs ?? 0;
    return message;
  },
};

function createBaseKeyTapSequenceStep(): KeyTapSequenceStep {
  return { packedKeys: new Uint8Array(0) };
}

export const KeyTapSequenceStep: MessageFns<KeyTapSequenceStep> = {
  encode(message: KeyTapSequenceStep, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.packedKeys.length !== 0) {
      writer.uint32(10).bytes(message.packedKeys);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyTapSequenceStep {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyTapSequenceStep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.packedKeys = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<KeyTapSequenceStep>): KeyTapSequenceStep {
    return KeyTapSequenceStep.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyTapSequenceStep>): KeyTapSequenceStep {
    const message = createBaseKeyTapSequenceStep();
    message.packedKeys = object.packedKeys ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMacroStep(): MacroStep {
  return { down: undefined, up: undefined, tap: undefined, delay: undefined, keyTapSequence: undefined };
}

export const MacroStep: MessageFns<MacroStep> = {
  encode(message: MacroStep, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.down !== undefined) {
      BehaviorBinding.encode(message.down, writer.uint32(10).fork()).join();
    }
    if (message.up !== undefined) {
      BehaviorBinding.encode(message.up, writer.uint32(18).fork()).join();
    }
    if (message.tap !== undefined) {
      BehaviorBinding.encode(message.tap, writer.uint32(26).fork()).join();
    }
    if (message.delay !== undefined) {
      DelayStep.encode(message.delay, writer.uint32(34).fork()).join();
    }
    if (message.keyTapSequence !== undefined) {
      KeyTapSequenceStep.encode(message.keyTapSequence, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MacroStep {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMacroStep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.down = BehaviorBinding.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.up = BehaviorBinding.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tap = BehaviorBinding.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.delay = DelayStep.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.keyTapSequence = KeyTapSequenceStep.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<MacroStep>): MacroStep {
    return MacroStep.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MacroStep>): MacroStep {
    const message = createBaseMacroStep();
    message.down = (object.down !== undefined && object.down !== null)
      ? BehaviorBinding.fromPartial(object.down)
      : undefined;
    message.up = (object.up !== undefined && object.up !== null) ? BehaviorBinding.fromPartial(object.up) : undefined;
    message.tap = (object.tap !== undefined && object.tap !== null)
      ? BehaviorBinding.fromPartial(object.tap)
      : undefined;
    message.delay = (object.delay !== undefined && object.delay !== null)
      ? DelayStep.fromPartial(object.delay)
      : undefined;
    message.keyTapSequence = (object.keyTapSequence !== undefined && object.keyTapSequence !== null)
      ? KeyTapSequenceStep.fromPartial(object.keyTapSequence)
      : undefined;
    return message;
  },
};

function createBaseMacroDetail(): MacroDetail {
  return { slot: 0, name: "", steps: [], encodedSize: 0 };
}

export const MacroDetail: MessageFns<MacroDetail> = {
  encode(message: MacroDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.steps) {
      MacroStep.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.encodedSize !== 0) {
      writer.uint32(32).uint32(message.encodedSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MacroDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMacroDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.steps.push(MacroStep.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.encodedSize = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<MacroDetail>): MacroDetail {
    return MacroDetail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MacroDetail>): MacroDetail {
    const message = createBaseMacroDetail();
    message.slot = object.slot ?? 0;
    message.name = object.name ?? "";
    message.steps = object.steps?.map((e) => MacroStep.fromPartial(e)) || [];
    message.encodedSize = object.encodedSize ?? 0;
    return message;
  },
};

function createBaseSetMacroStepCountRequest(): SetMacroStepCountRequest {
  return { slot: 0, stepCount: 0, persist: false };
}

export const SetMacroStepCountRequest: MessageFns<SetMacroStepCountRequest> = {
  encode(message: SetMacroStepCountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.stepCount !== 0) {
      writer.uint32(16).uint32(message.stepCount);
    }
    if (message.persist !== false) {
      writer.uint32(24).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMacroStepCountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMacroStepCountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stepCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetMacroStepCountRequest>): SetMacroStepCountRequest {
    return SetMacroStepCountRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetMacroStepCountRequest>): SetMacroStepCountRequest {
    const message = createBaseSetMacroStepCountRequest();
    message.slot = object.slot ?? 0;
    message.stepCount = object.stepCount ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSetMacroStepRequest(): SetMacroStepRequest {
  return { slot: 0, stepIndex: 0, step: undefined, persist: false };
}

export const SetMacroStepRequest: MessageFns<SetMacroStepRequest> = {
  encode(message: SetMacroStepRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.stepIndex !== 0) {
      writer.uint32(16).uint32(message.stepIndex);
    }
    if (message.step !== undefined) {
      MacroStep.encode(message.step, writer.uint32(26).fork()).join();
    }
    if (message.persist !== false) {
      writer.uint32(32).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetMacroStepRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetMacroStepRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stepIndex = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.step = MacroStep.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetMacroStepRequest>): SetMacroStepRequest {
    return SetMacroStepRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetMacroStepRequest>): SetMacroStepRequest {
    const message = createBaseSetMacroStepRequest();
    message.slot = object.slot ?? 0;
    message.stepIndex = object.stepIndex ?? 0;
    message.step = (object.step !== undefined && object.step !== null) ? MacroStep.fromPartial(object.step) : undefined;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseAppendMacroStepRequest(): AppendMacroStepRequest {
  return { slot: 0, step: undefined, persist: false };
}

export const AppendMacroStepRequest: MessageFns<AppendMacroStepRequest> = {
  encode(message: AppendMacroStepRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.step !== undefined) {
      MacroStep.encode(message.step, writer.uint32(18).fork()).join();
    }
    if (message.persist !== false) {
      writer.uint32(24).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppendMacroStepRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppendMacroStepRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.step = MacroStep.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AppendMacroStepRequest>): AppendMacroStepRequest {
    return AppendMacroStepRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppendMacroStepRequest>): AppendMacroStepRequest {
    const message = createBaseAppendMacroStepRequest();
    message.slot = object.slot ?? 0;
    message.step = (object.step !== undefined && object.step !== null) ? MacroStep.fromPartial(object.step) : undefined;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSetTapMsRequest(): SetTapMsRequest {
  return { tapMs: 0, persist: false };
}

export const SetTapMsRequest: MessageFns<SetTapMsRequest> = {
  encode(message: SetTapMsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tapMs !== 0) {
      writer.uint32(8).uint32(message.tapMs);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetTapMsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetTapMsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tapMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetTapMsRequest>): SetTapMsRequest {
    return SetTapMsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetTapMsRequest>): SetTapMsRequest {
    const message = createBaseSetTapMsRequest();
    message.tapMs = object.tapMs ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseSaveMacrosRequest(): SaveMacrosRequest {
  return {};
}

export const SaveMacrosRequest: MessageFns<SaveMacrosRequest> = {
  encode(_: SaveMacrosRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SaveMacrosRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSaveMacrosRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SaveMacrosRequest>): SaveMacrosRequest {
    return SaveMacrosRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SaveMacrosRequest>): SaveMacrosRequest {
    const message = createBaseSaveMacrosRequest();
    return message;
  },
};

function createBaseDiscardMacrosRequest(): DiscardMacrosRequest {
  return {};
}

export const DiscardMacrosRequest: MessageFns<DiscardMacrosRequest> = {
  encode(_: DiscardMacrosRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscardMacrosRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscardMacrosRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DiscardMacrosRequest>): DiscardMacrosRequest {
    return DiscardMacrosRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<DiscardMacrosRequest>): DiscardMacrosRequest {
    const message = createBaseDiscardMacrosRequest();
    return message;
  },
};

function createBaseCreateMacroRequest(): CreateMacroRequest {
  return { name: "", persist: false };
}

export const CreateMacroRequest: MessageFns<CreateMacroRequest> = {
  encode(message: CreateMacroRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateMacroRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateMacroRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<CreateMacroRequest>): CreateMacroRequest {
    return CreateMacroRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateMacroRequest>): CreateMacroRequest {
    const message = createBaseCreateMacroRequest();
    message.name = object.name ?? "";
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseDeleteMacroRequest(): DeleteMacroRequest {
  return { name: "" };
}

export const DeleteMacroRequest: MessageFns<DeleteMacroRequest> = {
  encode(message: DeleteMacroRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteMacroRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteMacroRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DeleteMacroRequest>): DeleteMacroRequest {
    return DeleteMacroRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeleteMacroRequest>): DeleteMacroRequest {
    const message = createBaseDeleteMacroRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseRenameMacroRequest(): RenameMacroRequest {
  return { oldName: "", newName: "", persist: false };
}

export const RenameMacroRequest: MessageFns<RenameMacroRequest> = {
  encode(message: RenameMacroRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldName !== "") {
      writer.uint32(10).string(message.oldName);
    }
    if (message.newName !== "") {
      writer.uint32(18).string(message.newName);
    }
    if (message.persist !== false) {
      writer.uint32(24).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RenameMacroRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenameMacroRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oldName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<RenameMacroRequest>): RenameMacroRequest {
    return RenameMacroRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RenameMacroRequest>): RenameMacroRequest {
    const message = createBaseRenameMacroRequest();
    message.oldName = object.oldName ?? "";
    message.newName = object.newName ?? "";
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseResetMacroRequest(): ResetMacroRequest {
  return { slot: 0, persist: false };
}

export const ResetMacroRequest: MessageFns<ResetMacroRequest> = {
  encode(message: ResetMacroRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slot !== 0) {
      writer.uint32(8).uint32(message.slot);
    }
    if (message.persist !== false) {
      writer.uint32(16).bool(message.persist);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResetMacroRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetMacroRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.slot = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.persist = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ResetMacroRequest>): ResetMacroRequest {
    return ResetMacroRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResetMacroRequest>): ResetMacroRequest {
    const message = createBaseResetMacroRequest();
    message.slot = object.slot ?? 0;
    message.persist = object.persist ?? false;
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    listMacros: undefined,
    getMacro: undefined,
    setMacroStepCount: undefined,
    getMacroGlobalSettings: undefined,
    setTapMs: undefined,
    setMacroStep: undefined,
    saveMacros: undefined,
    discardMacros: undefined,
    appendMacroStep: undefined,
    createMacro: undefined,
    deleteMacro: undefined,
    renameMacro: undefined,
    resetMacro: undefined,
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.listMacros !== undefined) {
      ListMacrosRequest.encode(message.listMacros, writer.uint32(10).fork()).join();
    }
    if (message.getMacro !== undefined) {
      GetMacroRequest.encode(message.getMacro, writer.uint32(18).fork()).join();
    }
    if (message.setMacroStepCount !== undefined) {
      SetMacroStepCountRequest.encode(message.setMacroStepCount, writer.uint32(34).fork()).join();
    }
    if (message.getMacroGlobalSettings !== undefined) {
      GetMacroGlobalSettingsRequest.encode(message.getMacroGlobalSettings, writer.uint32(42).fork()).join();
    }
    if (message.setTapMs !== undefined) {
      SetTapMsRequest.encode(message.setTapMs, writer.uint32(50).fork()).join();
    }
    if (message.setMacroStep !== undefined) {
      SetMacroStepRequest.encode(message.setMacroStep, writer.uint32(58).fork()).join();
    }
    if (message.saveMacros !== undefined) {
      SaveMacrosRequest.encode(message.saveMacros, writer.uint32(74).fork()).join();
    }
    if (message.discardMacros !== undefined) {
      DiscardMacrosRequest.encode(message.discardMacros, writer.uint32(82).fork()).join();
    }
    if (message.appendMacroStep !== undefined) {
      AppendMacroStepRequest.encode(message.appendMacroStep, writer.uint32(90).fork()).join();
    }
    if (message.createMacro !== undefined) {
      CreateMacroRequest.encode(message.createMacro, writer.uint32(98).fork()).join();
    }
    if (message.deleteMacro !== undefined) {
      DeleteMacroRequest.encode(message.deleteMacro, writer.uint32(106).fork()).join();
    }
    if (message.renameMacro !== undefined) {
      RenameMacroRequest.encode(message.renameMacro, writer.uint32(114).fork()).join();
    }
    if (message.resetMacro !== undefined) {
      ResetMacroRequest.encode(message.resetMacro, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.listMacros = ListMacrosRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.getMacro = GetMacroRequest.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.setMacroStepCount = SetMacroStepCountRequest.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getMacroGlobalSettings = GetMacroGlobalSettingsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.setTapMs = SetTapMsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.setMacroStep = SetMacroStepRequest.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.saveMacros = SaveMacrosRequest.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.discardMacros = DiscardMacrosRequest.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.appendMacroStep = AppendMacroStepRequest.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createMacro = CreateMacroRequest.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.deleteMacro = DeleteMacroRequest.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.renameMacro = RenameMacroRequest.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.resetMacro = ResetMacroRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.listMacros = (object.listMacros !== undefined && object.listMacros !== null)
      ? ListMacrosRequest.fromPartial(object.listMacros)
      : undefined;
    message.getMacro = (object.getMacro !== undefined && object.getMacro !== null)
      ? GetMacroRequest.fromPartial(object.getMacro)
      : undefined;
    message.setMacroStepCount = (object.setMacroStepCount !== undefined && object.setMacroStepCount !== null)
      ? SetMacroStepCountRequest.fromPartial(object.setMacroStepCount)
      : undefined;
    message.getMacroGlobalSettings =
      (object.getMacroGlobalSettings !== undefined && object.getMacroGlobalSettings !== null)
        ? GetMacroGlobalSettingsRequest.fromPartial(object.getMacroGlobalSettings)
        : undefined;
    message.setTapMs = (object.setTapMs !== undefined && object.setTapMs !== null)
      ? SetTapMsRequest.fromPartial(object.setTapMs)
      : undefined;
    message.setMacroStep = (object.setMacroStep !== undefined && object.setMacroStep !== null)
      ? SetMacroStepRequest.fromPartial(object.setMacroStep)
      : undefined;
    message.saveMacros = (object.saveMacros !== undefined && object.saveMacros !== null)
      ? SaveMacrosRequest.fromPartial(object.saveMacros)
      : undefined;
    message.discardMacros = (object.discardMacros !== undefined && object.discardMacros !== null)
      ? DiscardMacrosRequest.fromPartial(object.discardMacros)
      : undefined;
    message.appendMacroStep = (object.appendMacroStep !== undefined && object.appendMacroStep !== null)
      ? AppendMacroStepRequest.fromPartial(object.appendMacroStep)
      : undefined;
    message.createMacro = (object.createMacro !== undefined && object.createMacro !== null)
      ? CreateMacroRequest.fromPartial(object.createMacro)
      : undefined;
    message.deleteMacro = (object.deleteMacro !== undefined && object.deleteMacro !== null)
      ? DeleteMacroRequest.fromPartial(object.deleteMacro)
      : undefined;
    message.renameMacro = (object.renameMacro !== undefined && object.renameMacro !== null)
      ? RenameMacroRequest.fromPartial(object.renameMacro)
      : undefined;
    message.resetMacro = (object.resetMacro !== undefined && object.resetMacro !== null)
      ? ResetMacroRequest.fromPartial(object.resetMacro)
      : undefined;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseStatusResponse(): StatusResponse {
  return { affectedCount: 0, message: "" };
}

export const StatusResponse: MessageFns<StatusResponse> = {
  encode(message: StatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.affectedCount !== 0) {
      writer.uint32(8).uint32(message.affectedCount);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.affectedCount = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<StatusResponse>): StatusResponse {
    return StatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusResponse>): StatusResponse {
    const message = createBaseStatusResponse();
    message.affectedCount = object.affectedCount ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseListMacrosResponse(): ListMacrosResponse {
  return { macros: [], maxMacroBytes: 0, maxNameLength: 0 };
}

export const ListMacrosResponse: MessageFns<ListMacrosResponse> = {
  encode(message: ListMacrosResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.macros) {
      MacroSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.maxMacroBytes !== 0) {
      writer.uint32(16).uint32(message.maxMacroBytes);
    }
    if (message.maxNameLength !== 0) {
      writer.uint32(24).uint32(message.maxNameLength);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMacrosResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMacrosResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.macros.push(MacroSummary.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxMacroBytes = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxNameLength = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListMacrosResponse>): ListMacrosResponse {
    return ListMacrosResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListMacrosResponse>): ListMacrosResponse {
    const message = createBaseListMacrosResponse();
    message.macros = object.macros?.map((e) => MacroSummary.fromPartial(e)) || [];
    message.maxMacroBytes = object.maxMacroBytes ?? 0;
    message.maxNameLength = object.maxNameLength ?? 0;
    return message;
  },
};

function createBaseGetMacroResponse(): GetMacroResponse {
  return { macro: undefined };
}

export const GetMacroResponse: MessageFns<GetMacroResponse> = {
  encode(message: GetMacroResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.macro !== undefined) {
      MacroDetail.encode(message.macro, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMacroResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMacroResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.macro = MacroDetail.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetMacroResponse>): GetMacroResponse {
    return GetMacroResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMacroResponse>): GetMacroResponse {
    const message = createBaseGetMacroResponse();
    message.macro = (object.macro !== undefined && object.macro !== null)
      ? MacroDetail.fromPartial(object.macro)
      : undefined;
    return message;
  },
};

function createBaseMacroGlobalSettings(): MacroGlobalSettings {
  return { tapMs: 0, maxEntries: 0, keyPressBehaviorId: 0, poolBytesTotal: 0, poolBytesUsed: 0 };
}

export const MacroGlobalSettings: MessageFns<MacroGlobalSettings> = {
  encode(message: MacroGlobalSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tapMs !== 0) {
      writer.uint32(8).uint32(message.tapMs);
    }
    if (message.maxEntries !== 0) {
      writer.uint32(16).uint32(message.maxEntries);
    }
    if (message.keyPressBehaviorId !== 0) {
      writer.uint32(24).uint32(message.keyPressBehaviorId);
    }
    if (message.poolBytesTotal !== 0) {
      writer.uint32(32).uint32(message.poolBytesTotal);
    }
    if (message.poolBytesUsed !== 0) {
      writer.uint32(40).uint32(message.poolBytesUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MacroGlobalSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMacroGlobalSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tapMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxEntries = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.keyPressBehaviorId = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.poolBytesTotal = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.poolBytesUsed = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<MacroGlobalSettings>): MacroGlobalSettings {
    return MacroGlobalSettings.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MacroGlobalSettings>): MacroGlobalSettings {
    const message = createBaseMacroGlobalSettings();
    message.tapMs = object.tapMs ?? 0;
    message.maxEntries = object.maxEntries ?? 0;
    message.keyPressBehaviorId = object.keyPressBehaviorId ?? 0;
    message.poolBytesTotal = object.poolBytesTotal ?? 0;
    message.poolBytesUsed = object.poolBytesUsed ?? 0;
    return message;
  },
};

function createBaseGetMacroGlobalSettingsResponse(): GetMacroGlobalSettingsResponse {
  return { settings: undefined };
}

export const GetMacroGlobalSettingsResponse: MessageFns<GetMacroGlobalSettingsResponse> = {
  encode(message: GetMacroGlobalSettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settings !== undefined) {
      MacroGlobalSettings.encode(message.settings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMacroGlobalSettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMacroGlobalSettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.settings = MacroGlobalSettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetMacroGlobalSettingsResponse>): GetMacroGlobalSettingsResponse {
    return GetMacroGlobalSettingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMacroGlobalSettingsResponse>): GetMacroGlobalSettingsResponse {
    const message = createBaseGetMacroGlobalSettingsResponse();
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? MacroGlobalSettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    error: undefined,
    status: undefined,
    listMacros: undefined,
    getMacro: undefined,
    getMacroGlobalSettings: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.status !== undefined) {
      StatusResponse.encode(message.status, writer.uint32(18).fork()).join();
    }
    if (message.listMacros !== undefined) {
      ListMacrosResponse.encode(message.listMacros, writer.uint32(26).fork()).join();
    }
    if (message.getMacro !== undefined) {
      GetMacroResponse.encode(message.getMacro, writer.uint32(34).fork()).join();
    }
    if (message.getMacroGlobalSettings !== undefined) {
      GetMacroGlobalSettingsResponse.encode(message.getMacroGlobalSettings, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = StatusResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.listMacros = ListMacrosResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.getMacro = GetMacroResponse.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getMacroGlobalSettings = GetMacroGlobalSettingsResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? StatusResponse.fromPartial(object.status)
      : undefined;
    message.listMacros = (object.listMacros !== undefined && object.listMacros !== null)
      ? ListMacrosResponse.fromPartial(object.listMacros)
      : undefined;
    message.getMacro = (object.getMacro !== undefined && object.getMacro !== null)
      ? GetMacroResponse.fromPartial(object.getMacro)
      : undefined;
    message.getMacroGlobalSettings =
      (object.getMacroGlobalSettings !== undefined && object.getMacroGlobalSettings !== null)
        ? GetMacroGlobalSettingsResponse.fromPartial(object.getMacroGlobalSettings)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
