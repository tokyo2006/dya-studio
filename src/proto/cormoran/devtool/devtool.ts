// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               unknown
// source: cormoran/devtool/devtool.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cormoran.devtool";

export const StudioLockState = {
  STUDIO_LOCK_STATE_UNSPECIFIED: 0,
  STUDIO_LOCK_STATE_LOCKED: 1,
  STUDIO_LOCK_STATE_UNLOCKED: 2,
  UNRECOGNIZED: -1,
} as const;

export type StudioLockState = typeof StudioLockState[keyof typeof StudioLockState];

export namespace StudioLockState {
  export type STUDIO_LOCK_STATE_UNSPECIFIED = typeof StudioLockState.STUDIO_LOCK_STATE_UNSPECIFIED;
  export type STUDIO_LOCK_STATE_LOCKED = typeof StudioLockState.STUDIO_LOCK_STATE_LOCKED;
  export type STUDIO_LOCK_STATE_UNLOCKED = typeof StudioLockState.STUDIO_LOCK_STATE_UNLOCKED;
  export type UNRECOGNIZED = typeof StudioLockState.UNRECOGNIZED;
}

export const DevtoolEventType = {
  DEVTOOL_EVENT_TYPE_UNSPECIFIED: 0,
  DEVTOOL_EVENT_TYPE_POSITION_STATE_CHANGED: 1,
  DEVTOOL_EVENT_TYPE_KEYCODE_STATE_CHANGED: 2,
  DEVTOOL_EVENT_TYPE_LAYER_STATE_CHANGED: 3,
  DEVTOOL_EVENT_TYPE_MODIFIERS_STATE_CHANGED: 4,
  UNRECOGNIZED: -1,
} as const;

export type DevtoolEventType = typeof DevtoolEventType[keyof typeof DevtoolEventType];

export namespace DevtoolEventType {
  export type DEVTOOL_EVENT_TYPE_UNSPECIFIED = typeof DevtoolEventType.DEVTOOL_EVENT_TYPE_UNSPECIFIED;
  export type DEVTOOL_EVENT_TYPE_POSITION_STATE_CHANGED =
    typeof DevtoolEventType.DEVTOOL_EVENT_TYPE_POSITION_STATE_CHANGED;
  export type DEVTOOL_EVENT_TYPE_KEYCODE_STATE_CHANGED =
    typeof DevtoolEventType.DEVTOOL_EVENT_TYPE_KEYCODE_STATE_CHANGED;
  export type DEVTOOL_EVENT_TYPE_LAYER_STATE_CHANGED = typeof DevtoolEventType.DEVTOOL_EVENT_TYPE_LAYER_STATE_CHANGED;
  export type DEVTOOL_EVENT_TYPE_MODIFIERS_STATE_CHANGED =
    typeof DevtoolEventType.DEVTOOL_EVENT_TYPE_MODIFIERS_STATE_CHANGED;
  export type UNRECOGNIZED = typeof DevtoolEventType.UNRECOGNIZED;
}

export const LogLevel = {
  LOG_LEVEL_UNSPECIFIED: 0,
  LOG_LEVEL_ERR: 1,
  LOG_LEVEL_WRN: 2,
  LOG_LEVEL_INF: 3,
  LOG_LEVEL_DBG: 4,
  UNRECOGNIZED: -1,
} as const;

export type LogLevel = typeof LogLevel[keyof typeof LogLevel];

export namespace LogLevel {
  export type LOG_LEVEL_UNSPECIFIED = typeof LogLevel.LOG_LEVEL_UNSPECIFIED;
  export type LOG_LEVEL_ERR = typeof LogLevel.LOG_LEVEL_ERR;
  export type LOG_LEVEL_WRN = typeof LogLevel.LOG_LEVEL_WRN;
  export type LOG_LEVEL_INF = typeof LogLevel.LOG_LEVEL_INF;
  export type LOG_LEVEL_DBG = typeof LogLevel.LOG_LEVEL_DBG;
  export type UNRECOGNIZED = typeof LogLevel.UNRECOGNIZED;
}

export interface SetStudioLockStateRequest {
  state: StudioLockState;
}

export interface SetStudioLockStateResponse {
  state: StudioLockState;
}

export interface GetStudioLockStateRequest {
}

export interface GetStudioLockStateResponse {
  state: StudioLockState;
}

export interface EnterBootloaderRequest {
}

export interface EnterBootloaderResponse {
}

export interface RebootRequest {
}

export interface RebootResponse {
}

/** Get the currently active layers. Mirrors zmk_keymap_layer_state(). */
export interface GetLayerStateRequest {
}

export interface GetLayerStateResponse {
  /** Bitmask, bit N set means layer id N is active. */
  activeLayers: number;
  /** zmk_keymap_highest_layer_active(); ZMK_KEYMAP_LAYER_ID_INVAL (0xFF) if none. */
  highestActiveLayer: number;
}

/**
 * Momentary activate/deactivate a layer, mirrors &mo / &to style behaviors
 * invoked directly instead of through a keymap binding.
 */
export interface SetLayerStateRequest {
  layer: number;
  active: boolean;
  locking: boolean;
}

export interface SetLayerStateResponse {
  activeLayers: number;
}

export interface ToggleLayerRequest {
  layer: number;
  locking: boolean;
}

export interface ToggleLayerResponse {
  activeLayers: number;
}

export interface InjectKeyRequest {
  position: number;
  pressed: boolean;
}

export interface InjectKeyResponse {
}

/**
 * Press then schedule a release after hold_ms. Only one tap can be pending
 * at a time; a second tap_key request while one is in flight is rejected.
 */
export interface TapKeyRequest {
  position: number;
  holdMs: number;
}

export interface TapKeyResponse {
}

export interface PositionStateChangedData {
  position: number;
  pressed: boolean;
}

export interface KeycodeStateChangedData {
  usagePage: number;
  keycode: number;
  pressed: boolean;
  implicitModifiers: number;
  explicitModifiers: number;
}

export interface LayerStateChangedData {
  layer: number;
  active: boolean;
  locked: boolean;
}

export interface ModifiersStateChangedData {
  modifiers: number;
  pressed: boolean;
}

export interface DevtoolEvent {
  timestampMs: number;
  type: DevtoolEventType;
  positionStateChanged?: PositionStateChangedData | undefined;
  keycodeStateChanged?: KeycodeStateChangedData | undefined;
  layerStateChanged?: LayerStateChangedData | undefined;
  modifiersStateChanged?: ModifiersStateChangedData | undefined;
}

/**
 * Bitmask of (1 << DevtoolEventType value) for the event types to capture.
 * 0 disables the tap and drops the buffered content.
 */
export interface SubscribeEventsRequest {
  eventTypeMask: number;
}

export interface SubscribeEventsResponse {
  eventTypeMask: number;
}

/**
 * Cursor based, same shape as GetLogsRequest/Response: pass back the
 * previous response's next_cursor to continue draining without gaps
 * (bar dropped_count when the buffer wrapped between polls).
 */
export interface GetEventsRequest {
  cursor: number;
}

export interface GetEventsResponse {
  events: DevtoolEvent[];
  nextCursor: number;
  droppedCount: number;
}

export interface ClearEventsRequest {
}

export interface ClearEventsResponse {
}

export interface LogRecord {
  timestampMs: number;
  level: LogLevel;
  source: string;
  message: string;
}

export interface GetLogsRequest {
  cursor: number;
}

export interface GetLogsResponse {
  records: LogRecord[];
  nextCursor: number;
  droppedCount: number;
}

export interface ClearLogsRequest {
}

export interface ClearLogsResponse {
}

/**
 * Runtime override of the capture backend's minimum level, either globally
 * (source empty) or for one log source (e.g. "my_driver"). Does not affect
 * what other backends (RTT/UART console) show.
 */
export interface SetLogCaptureFilterRequest {
  source: string;
  minLevel: LogLevel;
}

export interface SetLogCaptureFilterResponse {
}

/**
 * --- Streaming log notifications (push instead of poll). When enabled, the
 * firmware periodically drains new records on ZMK's low-priority work queue
 * and sends a LogStreamNotification, so a client does not have to poll
 * get_logs. That work (notification + protobuf encoding + transmit) never
 * runs on the logging subsystem's own thread. Uses the same capture
 * backend/filter as the poll path, so set_log_capture_filter still applies.
 * get_logs and streaming can be used together (each has its own independent
 * cursor). ---
 */
export interface SetLogStreamingRequest {
  enabled: boolean;
}

export interface SetLogStreamingResponse {
  enabled: boolean;
}

/**
 * Same record shape as GetLogsResponse, minus next_cursor (the stream keeps
 * its own cursor firmware-side).
 */
export interface LogStreamNotification {
  records: LogRecord[];
  /**
   * Records lost because the ring buffer wrapped before they could be sent
   * (the client fell behind or the transport was saturated).
   */
  droppedCount: number;
}

export interface Request {
  setStudioLockState?: SetStudioLockStateRequest | undefined;
  enterBootloader?: EnterBootloaderRequest | undefined;
  reboot?: RebootRequest | undefined;
  getStudioLockState?: GetStudioLockStateRequest | undefined;
  getLayerState?: GetLayerStateRequest | undefined;
  setLayerState?: SetLayerStateRequest | undefined;
  toggleLayer?: ToggleLayerRequest | undefined;
  injectKey?: InjectKeyRequest | undefined;
  tapKey?: TapKeyRequest | undefined;
  subscribeEvents?: SubscribeEventsRequest | undefined;
  getEvents?: GetEventsRequest | undefined;
  clearEvents?: ClearEventsRequest | undefined;
  getLogs?: GetLogsRequest | undefined;
  clearLogs?: ClearLogsRequest | undefined;
  setLogCaptureFilter?: SetLogCaptureFilterRequest | undefined;
  setLogStreaming?: SetLogStreamingRequest | undefined;
}

export interface ErrorResponse {
  message: string;
}

export interface Response {
  error?: ErrorResponse | undefined;
  setStudioLockState?: SetStudioLockStateResponse | undefined;
  enterBootloader?: EnterBootloaderResponse | undefined;
  reboot?: RebootResponse | undefined;
  getStudioLockState?: GetStudioLockStateResponse | undefined;
  getLayerState?: GetLayerStateResponse | undefined;
  setLayerState?: SetLayerStateResponse | undefined;
  toggleLayer?: ToggleLayerResponse | undefined;
  injectKey?: InjectKeyResponse | undefined;
  tapKey?: TapKeyResponse | undefined;
  subscribeEvents?: SubscribeEventsResponse | undefined;
  getEvents?: GetEventsResponse | undefined;
  clearEvents?: ClearEventsResponse | undefined;
  getLogs?: GetLogsResponse | undefined;
  clearLogs?: ClearLogsResponse | undefined;
  setLogCaptureFilter?: SetLogCaptureFilterResponse | undefined;
  setLogStreaming?: SetLogStreamingResponse | undefined;
}

/**
 * Custom-subsystem notification payload (the bytes carried in a Studio
 * CustomNotification for this subsystem). Decoded client-side to receive
 * streamed log records.
 */
export interface Notification {
  logStream?: LogStreamNotification | undefined;
}

function createBaseSetStudioLockStateRequest(): SetStudioLockStateRequest {
  return { state: 0 };
}

export const SetStudioLockStateRequest: MessageFns<SetStudioLockStateRequest> = {
  encode(message: SetStudioLockStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetStudioLockStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetStudioLockStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetStudioLockStateRequest>): SetStudioLockStateRequest {
    return SetStudioLockStateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetStudioLockStateRequest>): SetStudioLockStateRequest {
    const message = createBaseSetStudioLockStateRequest();
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseSetStudioLockStateResponse(): SetStudioLockStateResponse {
  return { state: 0 };
}

export const SetStudioLockStateResponse: MessageFns<SetStudioLockStateResponse> = {
  encode(message: SetStudioLockStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetStudioLockStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetStudioLockStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetStudioLockStateResponse>): SetStudioLockStateResponse {
    return SetStudioLockStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetStudioLockStateResponse>): SetStudioLockStateResponse {
    const message = createBaseSetStudioLockStateResponse();
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseGetStudioLockStateRequest(): GetStudioLockStateRequest {
  return {};
}

export const GetStudioLockStateRequest: MessageFns<GetStudioLockStateRequest> = {
  encode(_: GetStudioLockStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStudioLockStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStudioLockStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetStudioLockStateRequest>): GetStudioLockStateRequest {
    return GetStudioLockStateRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetStudioLockStateRequest>): GetStudioLockStateRequest {
    const message = createBaseGetStudioLockStateRequest();
    return message;
  },
};

function createBaseGetStudioLockStateResponse(): GetStudioLockStateResponse {
  return { state: 0 };
}

export const GetStudioLockStateResponse: MessageFns<GetStudioLockStateResponse> = {
  encode(message: GetStudioLockStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStudioLockStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStudioLockStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetStudioLockStateResponse>): GetStudioLockStateResponse {
    return GetStudioLockStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStudioLockStateResponse>): GetStudioLockStateResponse {
    const message = createBaseGetStudioLockStateResponse();
    message.state = object.state ?? 0;
    return message;
  },
};

function createBaseEnterBootloaderRequest(): EnterBootloaderRequest {
  return {};
}

export const EnterBootloaderRequest: MessageFns<EnterBootloaderRequest> = {
  encode(_: EnterBootloaderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnterBootloaderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnterBootloaderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<EnterBootloaderRequest>): EnterBootloaderRequest {
    return EnterBootloaderRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<EnterBootloaderRequest>): EnterBootloaderRequest {
    const message = createBaseEnterBootloaderRequest();
    return message;
  },
};

function createBaseEnterBootloaderResponse(): EnterBootloaderResponse {
  return {};
}

export const EnterBootloaderResponse: MessageFns<EnterBootloaderResponse> = {
  encode(_: EnterBootloaderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnterBootloaderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnterBootloaderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<EnterBootloaderResponse>): EnterBootloaderResponse {
    return EnterBootloaderResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<EnterBootloaderResponse>): EnterBootloaderResponse {
    const message = createBaseEnterBootloaderResponse();
    return message;
  },
};

function createBaseRebootRequest(): RebootRequest {
  return {};
}

export const RebootRequest: MessageFns<RebootRequest> = {
  encode(_: RebootRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RebootRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRebootRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<RebootRequest>): RebootRequest {
    return RebootRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RebootRequest>): RebootRequest {
    const message = createBaseRebootRequest();
    return message;
  },
};

function createBaseRebootResponse(): RebootResponse {
  return {};
}

export const RebootResponse: MessageFns<RebootResponse> = {
  encode(_: RebootResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RebootResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRebootResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<RebootResponse>): RebootResponse {
    return RebootResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RebootResponse>): RebootResponse {
    const message = createBaseRebootResponse();
    return message;
  },
};

function createBaseGetLayerStateRequest(): GetLayerStateRequest {
  return {};
}

export const GetLayerStateRequest: MessageFns<GetLayerStateRequest> = {
  encode(_: GetLayerStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLayerStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLayerStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetLayerStateRequest>): GetLayerStateRequest {
    return GetLayerStateRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetLayerStateRequest>): GetLayerStateRequest {
    const message = createBaseGetLayerStateRequest();
    return message;
  },
};

function createBaseGetLayerStateResponse(): GetLayerStateResponse {
  return { activeLayers: 0, highestActiveLayer: 0 };
}

export const GetLayerStateResponse: MessageFns<GetLayerStateResponse> = {
  encode(message: GetLayerStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeLayers !== 0) {
      writer.uint32(8).uint32(message.activeLayers);
    }
    if (message.highestActiveLayer !== 0) {
      writer.uint32(16).uint32(message.highestActiveLayer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLayerStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLayerStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activeLayers = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.highestActiveLayer = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetLayerStateResponse>): GetLayerStateResponse {
    return GetLayerStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLayerStateResponse>): GetLayerStateResponse {
    const message = createBaseGetLayerStateResponse();
    message.activeLayers = object.activeLayers ?? 0;
    message.highestActiveLayer = object.highestActiveLayer ?? 0;
    return message;
  },
};

function createBaseSetLayerStateRequest(): SetLayerStateRequest {
  return { layer: 0, active: false, locking: false };
}

export const SetLayerStateRequest: MessageFns<SetLayerStateRequest> = {
  encode(message: SetLayerStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layer !== 0) {
      writer.uint32(8).uint32(message.layer);
    }
    if (message.active !== false) {
      writer.uint32(16).bool(message.active);
    }
    if (message.locking !== false) {
      writer.uint32(24).bool(message.locking);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLayerStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLayerStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layer = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.locking = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLayerStateRequest>): SetLayerStateRequest {
    return SetLayerStateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetLayerStateRequest>): SetLayerStateRequest {
    const message = createBaseSetLayerStateRequest();
    message.layer = object.layer ?? 0;
    message.active = object.active ?? false;
    message.locking = object.locking ?? false;
    return message;
  },
};

function createBaseSetLayerStateResponse(): SetLayerStateResponse {
  return { activeLayers: 0 };
}

export const SetLayerStateResponse: MessageFns<SetLayerStateResponse> = {
  encode(message: SetLayerStateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeLayers !== 0) {
      writer.uint32(8).uint32(message.activeLayers);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLayerStateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLayerStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activeLayers = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLayerStateResponse>): SetLayerStateResponse {
    return SetLayerStateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetLayerStateResponse>): SetLayerStateResponse {
    const message = createBaseSetLayerStateResponse();
    message.activeLayers = object.activeLayers ?? 0;
    return message;
  },
};

function createBaseToggleLayerRequest(): ToggleLayerRequest {
  return { layer: 0, locking: false };
}

export const ToggleLayerRequest: MessageFns<ToggleLayerRequest> = {
  encode(message: ToggleLayerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layer !== 0) {
      writer.uint32(8).uint32(message.layer);
    }
    if (message.locking !== false) {
      writer.uint32(16).bool(message.locking);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToggleLayerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToggleLayerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layer = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.locking = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ToggleLayerRequest>): ToggleLayerRequest {
    return ToggleLayerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ToggleLayerRequest>): ToggleLayerRequest {
    const message = createBaseToggleLayerRequest();
    message.layer = object.layer ?? 0;
    message.locking = object.locking ?? false;
    return message;
  },
};

function createBaseToggleLayerResponse(): ToggleLayerResponse {
  return { activeLayers: 0 };
}

export const ToggleLayerResponse: MessageFns<ToggleLayerResponse> = {
  encode(message: ToggleLayerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activeLayers !== 0) {
      writer.uint32(8).uint32(message.activeLayers);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToggleLayerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToggleLayerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activeLayers = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ToggleLayerResponse>): ToggleLayerResponse {
    return ToggleLayerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ToggleLayerResponse>): ToggleLayerResponse {
    const message = createBaseToggleLayerResponse();
    message.activeLayers = object.activeLayers ?? 0;
    return message;
  },
};

function createBaseInjectKeyRequest(): InjectKeyRequest {
  return { position: 0, pressed: false };
}

export const InjectKeyRequest: MessageFns<InjectKeyRequest> = {
  encode(message: InjectKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== 0) {
      writer.uint32(8).uint32(message.position);
    }
    if (message.pressed !== false) {
      writer.uint32(16).bool(message.pressed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InjectKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInjectKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.position = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pressed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<InjectKeyRequest>): InjectKeyRequest {
    return InjectKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InjectKeyRequest>): InjectKeyRequest {
    const message = createBaseInjectKeyRequest();
    message.position = object.position ?? 0;
    message.pressed = object.pressed ?? false;
    return message;
  },
};

function createBaseInjectKeyResponse(): InjectKeyResponse {
  return {};
}

export const InjectKeyResponse: MessageFns<InjectKeyResponse> = {
  encode(_: InjectKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InjectKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInjectKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<InjectKeyResponse>): InjectKeyResponse {
    return InjectKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<InjectKeyResponse>): InjectKeyResponse {
    const message = createBaseInjectKeyResponse();
    return message;
  },
};

function createBaseTapKeyRequest(): TapKeyRequest {
  return { position: 0, holdMs: 0 };
}

export const TapKeyRequest: MessageFns<TapKeyRequest> = {
  encode(message: TapKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== 0) {
      writer.uint32(8).uint32(message.position);
    }
    if (message.holdMs !== 0) {
      writer.uint32(16).uint32(message.holdMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TapKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTapKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.position = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.holdMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<TapKeyRequest>): TapKeyRequest {
    return TapKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TapKeyRequest>): TapKeyRequest {
    const message = createBaseTapKeyRequest();
    message.position = object.position ?? 0;
    message.holdMs = object.holdMs ?? 0;
    return message;
  },
};

function createBaseTapKeyResponse(): TapKeyResponse {
  return {};
}

export const TapKeyResponse: MessageFns<TapKeyResponse> = {
  encode(_: TapKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TapKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTapKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<TapKeyResponse>): TapKeyResponse {
    return TapKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<TapKeyResponse>): TapKeyResponse {
    const message = createBaseTapKeyResponse();
    return message;
  },
};

function createBasePositionStateChangedData(): PositionStateChangedData {
  return { position: 0, pressed: false };
}

export const PositionStateChangedData: MessageFns<PositionStateChangedData> = {
  encode(message: PositionStateChangedData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.position !== 0) {
      writer.uint32(8).uint32(message.position);
    }
    if (message.pressed !== false) {
      writer.uint32(16).bool(message.pressed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PositionStateChangedData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePositionStateChangedData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.position = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pressed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<PositionStateChangedData>): PositionStateChangedData {
    return PositionStateChangedData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PositionStateChangedData>): PositionStateChangedData {
    const message = createBasePositionStateChangedData();
    message.position = object.position ?? 0;
    message.pressed = object.pressed ?? false;
    return message;
  },
};

function createBaseKeycodeStateChangedData(): KeycodeStateChangedData {
  return { usagePage: 0, keycode: 0, pressed: false, implicitModifiers: 0, explicitModifiers: 0 };
}

export const KeycodeStateChangedData: MessageFns<KeycodeStateChangedData> = {
  encode(message: KeycodeStateChangedData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.usagePage !== 0) {
      writer.uint32(8).uint32(message.usagePage);
    }
    if (message.keycode !== 0) {
      writer.uint32(16).uint32(message.keycode);
    }
    if (message.pressed !== false) {
      writer.uint32(24).bool(message.pressed);
    }
    if (message.implicitModifiers !== 0) {
      writer.uint32(32).uint32(message.implicitModifiers);
    }
    if (message.explicitModifiers !== 0) {
      writer.uint32(40).uint32(message.explicitModifiers);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeycodeStateChangedData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeycodeStateChangedData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.usagePage = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.keycode = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pressed = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.implicitModifiers = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.explicitModifiers = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<KeycodeStateChangedData>): KeycodeStateChangedData {
    return KeycodeStateChangedData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeycodeStateChangedData>): KeycodeStateChangedData {
    const message = createBaseKeycodeStateChangedData();
    message.usagePage = object.usagePage ?? 0;
    message.keycode = object.keycode ?? 0;
    message.pressed = object.pressed ?? false;
    message.implicitModifiers = object.implicitModifiers ?? 0;
    message.explicitModifiers = object.explicitModifiers ?? 0;
    return message;
  },
};

function createBaseLayerStateChangedData(): LayerStateChangedData {
  return { layer: 0, active: false, locked: false };
}

export const LayerStateChangedData: MessageFns<LayerStateChangedData> = {
  encode(message: LayerStateChangedData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.layer !== 0) {
      writer.uint32(8).uint32(message.layer);
    }
    if (message.active !== false) {
      writer.uint32(16).bool(message.active);
    }
    if (message.locked !== false) {
      writer.uint32(24).bool(message.locked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LayerStateChangedData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayerStateChangedData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.layer = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.locked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<LayerStateChangedData>): LayerStateChangedData {
    return LayerStateChangedData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LayerStateChangedData>): LayerStateChangedData {
    const message = createBaseLayerStateChangedData();
    message.layer = object.layer ?? 0;
    message.active = object.active ?? false;
    message.locked = object.locked ?? false;
    return message;
  },
};

function createBaseModifiersStateChangedData(): ModifiersStateChangedData {
  return { modifiers: 0, pressed: false };
}

export const ModifiersStateChangedData: MessageFns<ModifiersStateChangedData> = {
  encode(message: ModifiersStateChangedData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modifiers !== 0) {
      writer.uint32(8).uint32(message.modifiers);
    }
    if (message.pressed !== false) {
      writer.uint32(16).bool(message.pressed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifiersStateChangedData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifiersStateChangedData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.modifiers = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pressed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ModifiersStateChangedData>): ModifiersStateChangedData {
    return ModifiersStateChangedData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifiersStateChangedData>): ModifiersStateChangedData {
    const message = createBaseModifiersStateChangedData();
    message.modifiers = object.modifiers ?? 0;
    message.pressed = object.pressed ?? false;
    return message;
  },
};

function createBaseDevtoolEvent(): DevtoolEvent {
  return {
    timestampMs: 0,
    type: 0,
    positionStateChanged: undefined,
    keycodeStateChanged: undefined,
    layerStateChanged: undefined,
    modifiersStateChanged: undefined,
  };
}

export const DevtoolEvent: MessageFns<DevtoolEvent> = {
  encode(message: DevtoolEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestampMs !== 0) {
      writer.uint32(8).uint32(message.timestampMs);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.positionStateChanged !== undefined) {
      PositionStateChangedData.encode(message.positionStateChanged, writer.uint32(26).fork()).join();
    }
    if (message.keycodeStateChanged !== undefined) {
      KeycodeStateChangedData.encode(message.keycodeStateChanged, writer.uint32(34).fork()).join();
    }
    if (message.layerStateChanged !== undefined) {
      LayerStateChangedData.encode(message.layerStateChanged, writer.uint32(42).fork()).join();
    }
    if (message.modifiersStateChanged !== undefined) {
      ModifiersStateChangedData.encode(message.modifiersStateChanged, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DevtoolEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevtoolEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestampMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.positionStateChanged = PositionStateChangedData.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.keycodeStateChanged = KeycodeStateChangedData.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.layerStateChanged = LayerStateChangedData.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.modifiersStateChanged = ModifiersStateChangedData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DevtoolEvent>): DevtoolEvent {
    return DevtoolEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DevtoolEvent>): DevtoolEvent {
    const message = createBaseDevtoolEvent();
    message.timestampMs = object.timestampMs ?? 0;
    message.type = object.type ?? 0;
    message.positionStateChanged = (object.positionStateChanged !== undefined && object.positionStateChanged !== null)
      ? PositionStateChangedData.fromPartial(object.positionStateChanged)
      : undefined;
    message.keycodeStateChanged = (object.keycodeStateChanged !== undefined && object.keycodeStateChanged !== null)
      ? KeycodeStateChangedData.fromPartial(object.keycodeStateChanged)
      : undefined;
    message.layerStateChanged = (object.layerStateChanged !== undefined && object.layerStateChanged !== null)
      ? LayerStateChangedData.fromPartial(object.layerStateChanged)
      : undefined;
    message.modifiersStateChanged =
      (object.modifiersStateChanged !== undefined && object.modifiersStateChanged !== null)
        ? ModifiersStateChangedData.fromPartial(object.modifiersStateChanged)
        : undefined;
    return message;
  },
};

function createBaseSubscribeEventsRequest(): SubscribeEventsRequest {
  return { eventTypeMask: 0 };
}

export const SubscribeEventsRequest: MessageFns<SubscribeEventsRequest> = {
  encode(message: SubscribeEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventTypeMask !== 0) {
      writer.uint32(8).uint32(message.eventTypeMask);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventTypeMask = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SubscribeEventsRequest>): SubscribeEventsRequest {
    return SubscribeEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubscribeEventsRequest>): SubscribeEventsRequest {
    const message = createBaseSubscribeEventsRequest();
    message.eventTypeMask = object.eventTypeMask ?? 0;
    return message;
  },
};

function createBaseSubscribeEventsResponse(): SubscribeEventsResponse {
  return { eventTypeMask: 0 };
}

export const SubscribeEventsResponse: MessageFns<SubscribeEventsResponse> = {
  encode(message: SubscribeEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventTypeMask !== 0) {
      writer.uint32(8).uint32(message.eventTypeMask);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.eventTypeMask = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SubscribeEventsResponse>): SubscribeEventsResponse {
    return SubscribeEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubscribeEventsResponse>): SubscribeEventsResponse {
    const message = createBaseSubscribeEventsResponse();
    message.eventTypeMask = object.eventTypeMask ?? 0;
    return message;
  },
};

function createBaseGetEventsRequest(): GetEventsRequest {
  return { cursor: 0 };
}

export const GetEventsRequest: MessageFns<GetEventsRequest> = {
  encode(message: GetEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== 0) {
      writer.uint32(8).uint32(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cursor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetEventsRequest>): GetEventsRequest {
    return GetEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsRequest>): GetEventsRequest {
    const message = createBaseGetEventsRequest();
    message.cursor = object.cursor ?? 0;
    return message;
  },
};

function createBaseGetEventsResponse(): GetEventsResponse {
  return { events: [], nextCursor: 0, droppedCount: 0 };
}

export const GetEventsResponse: MessageFns<GetEventsResponse> = {
  encode(message: GetEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      DevtoolEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== 0) {
      writer.uint32(16).uint32(message.nextCursor);
    }
    if (message.droppedCount !== 0) {
      writer.uint32(24).uint32(message.droppedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(DevtoolEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextCursor = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.droppedCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetEventsResponse>): GetEventsResponse {
    return GetEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetEventsResponse>): GetEventsResponse {
    const message = createBaseGetEventsResponse();
    message.events = object.events?.map((e) => DevtoolEvent.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? 0;
    message.droppedCount = object.droppedCount ?? 0;
    return message;
  },
};

function createBaseClearEventsRequest(): ClearEventsRequest {
  return {};
}

export const ClearEventsRequest: MessageFns<ClearEventsRequest> = {
  encode(_: ClearEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearEventsRequest>): ClearEventsRequest {
    return ClearEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClearEventsRequest>): ClearEventsRequest {
    const message = createBaseClearEventsRequest();
    return message;
  },
};

function createBaseClearEventsResponse(): ClearEventsResponse {
  return {};
}

export const ClearEventsResponse: MessageFns<ClearEventsResponse> = {
  encode(_: ClearEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearEventsResponse>): ClearEventsResponse {
    return ClearEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClearEventsResponse>): ClearEventsResponse {
    const message = createBaseClearEventsResponse();
    return message;
  },
};

function createBaseLogRecord(): LogRecord {
  return { timestampMs: 0, level: 0, source: "", message: "" };
}

export const LogRecord: MessageFns<LogRecord> = {
  encode(message: LogRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestampMs !== 0) {
      writer.uint32(8).uint32(message.timestampMs);
    }
    if (message.level !== 0) {
      writer.uint32(16).int32(message.level);
    }
    if (message.source !== "") {
      writer.uint32(26).string(message.source);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestampMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.level = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<LogRecord>): LogRecord {
    return LogRecord.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogRecord>): LogRecord {
    const message = createBaseLogRecord();
    message.timestampMs = object.timestampMs ?? 0;
    message.level = object.level ?? 0;
    message.source = object.source ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseGetLogsRequest(): GetLogsRequest {
  return { cursor: 0 };
}

export const GetLogsRequest: MessageFns<GetLogsRequest> = {
  encode(message: GetLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cursor !== 0) {
      writer.uint32(8).uint32(message.cursor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cursor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetLogsRequest>): GetLogsRequest {
    return GetLogsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLogsRequest>): GetLogsRequest {
    const message = createBaseGetLogsRequest();
    message.cursor = object.cursor ?? 0;
    return message;
  },
};

function createBaseGetLogsResponse(): GetLogsResponse {
  return { records: [], nextCursor: 0, droppedCount: 0 };
}

export const GetLogsResponse: MessageFns<GetLogsResponse> = {
  encode(message: GetLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.records) {
      LogRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextCursor !== 0) {
      writer.uint32(16).uint32(message.nextCursor);
    }
    if (message.droppedCount !== 0) {
      writer.uint32(24).uint32(message.droppedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.records.push(LogRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextCursor = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.droppedCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetLogsResponse>): GetLogsResponse {
    return GetLogsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetLogsResponse>): GetLogsResponse {
    const message = createBaseGetLogsResponse();
    message.records = object.records?.map((e) => LogRecord.fromPartial(e)) || [];
    message.nextCursor = object.nextCursor ?? 0;
    message.droppedCount = object.droppedCount ?? 0;
    return message;
  },
};

function createBaseClearLogsRequest(): ClearLogsRequest {
  return {};
}

export const ClearLogsRequest: MessageFns<ClearLogsRequest> = {
  encode(_: ClearLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearLogsRequest>): ClearLogsRequest {
    return ClearLogsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClearLogsRequest>): ClearLogsRequest {
    const message = createBaseClearLogsRequest();
    return message;
  },
};

function createBaseClearLogsResponse(): ClearLogsResponse {
  return {};
}

export const ClearLogsResponse: MessageFns<ClearLogsResponse> = {
  encode(_: ClearLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearLogsResponse>): ClearLogsResponse {
    return ClearLogsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClearLogsResponse>): ClearLogsResponse {
    const message = createBaseClearLogsResponse();
    return message;
  },
};

function createBaseSetLogCaptureFilterRequest(): SetLogCaptureFilterRequest {
  return { source: "", minLevel: 0 };
}

export const SetLogCaptureFilterRequest: MessageFns<SetLogCaptureFilterRequest> = {
  encode(message: SetLogCaptureFilterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.minLevel !== 0) {
      writer.uint32(16).int32(message.minLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLogCaptureFilterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLogCaptureFilterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minLevel = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLogCaptureFilterRequest>): SetLogCaptureFilterRequest {
    return SetLogCaptureFilterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetLogCaptureFilterRequest>): SetLogCaptureFilterRequest {
    const message = createBaseSetLogCaptureFilterRequest();
    message.source = object.source ?? "";
    message.minLevel = object.minLevel ?? 0;
    return message;
  },
};

function createBaseSetLogCaptureFilterResponse(): SetLogCaptureFilterResponse {
  return {};
}

export const SetLogCaptureFilterResponse: MessageFns<SetLogCaptureFilterResponse> = {
  encode(_: SetLogCaptureFilterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLogCaptureFilterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLogCaptureFilterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLogCaptureFilterResponse>): SetLogCaptureFilterResponse {
    return SetLogCaptureFilterResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SetLogCaptureFilterResponse>): SetLogCaptureFilterResponse {
    const message = createBaseSetLogCaptureFilterResponse();
    return message;
  },
};

function createBaseSetLogStreamingRequest(): SetLogStreamingRequest {
  return { enabled: false };
}

export const SetLogStreamingRequest: MessageFns<SetLogStreamingRequest> = {
  encode(message: SetLogStreamingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLogStreamingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLogStreamingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLogStreamingRequest>): SetLogStreamingRequest {
    return SetLogStreamingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetLogStreamingRequest>): SetLogStreamingRequest {
    const message = createBaseSetLogStreamingRequest();
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseSetLogStreamingResponse(): SetLogStreamingResponse {
  return { enabled: false };
}

export const SetLogStreamingResponse: MessageFns<SetLogStreamingResponse> = {
  encode(message: SetLogStreamingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetLogStreamingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetLogStreamingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetLogStreamingResponse>): SetLogStreamingResponse {
    return SetLogStreamingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetLogStreamingResponse>): SetLogStreamingResponse {
    const message = createBaseSetLogStreamingResponse();
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseLogStreamNotification(): LogStreamNotification {
  return { records: [], droppedCount: 0 };
}

export const LogStreamNotification: MessageFns<LogStreamNotification> = {
  encode(message: LogStreamNotification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.records) {
      LogRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.droppedCount !== 0) {
      writer.uint32(16).uint32(message.droppedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogStreamNotification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogStreamNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.records.push(LogRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.droppedCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<LogStreamNotification>): LogStreamNotification {
    return LogStreamNotification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogStreamNotification>): LogStreamNotification {
    const message = createBaseLogStreamNotification();
    message.records = object.records?.map((e) => LogRecord.fromPartial(e)) || [];
    message.droppedCount = object.droppedCount ?? 0;
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    setStudioLockState: undefined,
    enterBootloader: undefined,
    reboot: undefined,
    getStudioLockState: undefined,
    getLayerState: undefined,
    setLayerState: undefined,
    toggleLayer: undefined,
    injectKey: undefined,
    tapKey: undefined,
    subscribeEvents: undefined,
    getEvents: undefined,
    clearEvents: undefined,
    getLogs: undefined,
    clearLogs: undefined,
    setLogCaptureFilter: undefined,
    setLogStreaming: undefined,
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.setStudioLockState !== undefined) {
      SetStudioLockStateRequest.encode(message.setStudioLockState, writer.uint32(10).fork()).join();
    }
    if (message.enterBootloader !== undefined) {
      EnterBootloaderRequest.encode(message.enterBootloader, writer.uint32(18).fork()).join();
    }
    if (message.reboot !== undefined) {
      RebootRequest.encode(message.reboot, writer.uint32(26).fork()).join();
    }
    if (message.getStudioLockState !== undefined) {
      GetStudioLockStateRequest.encode(message.getStudioLockState, writer.uint32(34).fork()).join();
    }
    if (message.getLayerState !== undefined) {
      GetLayerStateRequest.encode(message.getLayerState, writer.uint32(42).fork()).join();
    }
    if (message.setLayerState !== undefined) {
      SetLayerStateRequest.encode(message.setLayerState, writer.uint32(50).fork()).join();
    }
    if (message.toggleLayer !== undefined) {
      ToggleLayerRequest.encode(message.toggleLayer, writer.uint32(58).fork()).join();
    }
    if (message.injectKey !== undefined) {
      InjectKeyRequest.encode(message.injectKey, writer.uint32(66).fork()).join();
    }
    if (message.tapKey !== undefined) {
      TapKeyRequest.encode(message.tapKey, writer.uint32(74).fork()).join();
    }
    if (message.subscribeEvents !== undefined) {
      SubscribeEventsRequest.encode(message.subscribeEvents, writer.uint32(82).fork()).join();
    }
    if (message.getEvents !== undefined) {
      GetEventsRequest.encode(message.getEvents, writer.uint32(90).fork()).join();
    }
    if (message.clearEvents !== undefined) {
      ClearEventsRequest.encode(message.clearEvents, writer.uint32(98).fork()).join();
    }
    if (message.getLogs !== undefined) {
      GetLogsRequest.encode(message.getLogs, writer.uint32(106).fork()).join();
    }
    if (message.clearLogs !== undefined) {
      ClearLogsRequest.encode(message.clearLogs, writer.uint32(114).fork()).join();
    }
    if (message.setLogCaptureFilter !== undefined) {
      SetLogCaptureFilterRequest.encode(message.setLogCaptureFilter, writer.uint32(122).fork()).join();
    }
    if (message.setLogStreaming !== undefined) {
      SetLogStreamingRequest.encode(message.setLogStreaming, writer.uint32(130).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.setStudioLockState = SetStudioLockStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.enterBootloader = EnterBootloaderRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reboot = RebootRequest.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.getStudioLockState = GetStudioLockStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getLayerState = GetLayerStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.setLayerState = SetLayerStateRequest.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.toggleLayer = ToggleLayerRequest.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.injectKey = InjectKeyRequest.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.tapKey = TapKeyRequest.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.subscribeEvents = SubscribeEventsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.getEvents = GetEventsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.clearEvents = ClearEventsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.getLogs = GetLogsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.clearLogs = ClearLogsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.setLogCaptureFilter = SetLogCaptureFilterRequest.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.setLogStreaming = SetLogStreamingRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.setStudioLockState = (object.setStudioLockState !== undefined && object.setStudioLockState !== null)
      ? SetStudioLockStateRequest.fromPartial(object.setStudioLockState)
      : undefined;
    message.enterBootloader = (object.enterBootloader !== undefined && object.enterBootloader !== null)
      ? EnterBootloaderRequest.fromPartial(object.enterBootloader)
      : undefined;
    message.reboot = (object.reboot !== undefined && object.reboot !== null)
      ? RebootRequest.fromPartial(object.reboot)
      : undefined;
    message.getStudioLockState = (object.getStudioLockState !== undefined && object.getStudioLockState !== null)
      ? GetStudioLockStateRequest.fromPartial(object.getStudioLockState)
      : undefined;
    message.getLayerState = (object.getLayerState !== undefined && object.getLayerState !== null)
      ? GetLayerStateRequest.fromPartial(object.getLayerState)
      : undefined;
    message.setLayerState = (object.setLayerState !== undefined && object.setLayerState !== null)
      ? SetLayerStateRequest.fromPartial(object.setLayerState)
      : undefined;
    message.toggleLayer = (object.toggleLayer !== undefined && object.toggleLayer !== null)
      ? ToggleLayerRequest.fromPartial(object.toggleLayer)
      : undefined;
    message.injectKey = (object.injectKey !== undefined && object.injectKey !== null)
      ? InjectKeyRequest.fromPartial(object.injectKey)
      : undefined;
    message.tapKey = (object.tapKey !== undefined && object.tapKey !== null)
      ? TapKeyRequest.fromPartial(object.tapKey)
      : undefined;
    message.subscribeEvents = (object.subscribeEvents !== undefined && object.subscribeEvents !== null)
      ? SubscribeEventsRequest.fromPartial(object.subscribeEvents)
      : undefined;
    message.getEvents = (object.getEvents !== undefined && object.getEvents !== null)
      ? GetEventsRequest.fromPartial(object.getEvents)
      : undefined;
    message.clearEvents = (object.clearEvents !== undefined && object.clearEvents !== null)
      ? ClearEventsRequest.fromPartial(object.clearEvents)
      : undefined;
    message.getLogs = (object.getLogs !== undefined && object.getLogs !== null)
      ? GetLogsRequest.fromPartial(object.getLogs)
      : undefined;
    message.clearLogs = (object.clearLogs !== undefined && object.clearLogs !== null)
      ? ClearLogsRequest.fromPartial(object.clearLogs)
      : undefined;
    message.setLogCaptureFilter = (object.setLogCaptureFilter !== undefined && object.setLogCaptureFilter !== null)
      ? SetLogCaptureFilterRequest.fromPartial(object.setLogCaptureFilter)
      : undefined;
    message.setLogStreaming = (object.setLogStreaming !== undefined && object.setLogStreaming !== null)
      ? SetLogStreamingRequest.fromPartial(object.setLogStreaming)
      : undefined;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    error: undefined,
    setStudioLockState: undefined,
    enterBootloader: undefined,
    reboot: undefined,
    getStudioLockState: undefined,
    getLayerState: undefined,
    setLayerState: undefined,
    toggleLayer: undefined,
    injectKey: undefined,
    tapKey: undefined,
    subscribeEvents: undefined,
    getEvents: undefined,
    clearEvents: undefined,
    getLogs: undefined,
    clearLogs: undefined,
    setLogCaptureFilter: undefined,
    setLogStreaming: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.setStudioLockState !== undefined) {
      SetStudioLockStateResponse.encode(message.setStudioLockState, writer.uint32(18).fork()).join();
    }
    if (message.enterBootloader !== undefined) {
      EnterBootloaderResponse.encode(message.enterBootloader, writer.uint32(26).fork()).join();
    }
    if (message.reboot !== undefined) {
      RebootResponse.encode(message.reboot, writer.uint32(34).fork()).join();
    }
    if (message.getStudioLockState !== undefined) {
      GetStudioLockStateResponse.encode(message.getStudioLockState, writer.uint32(42).fork()).join();
    }
    if (message.getLayerState !== undefined) {
      GetLayerStateResponse.encode(message.getLayerState, writer.uint32(50).fork()).join();
    }
    if (message.setLayerState !== undefined) {
      SetLayerStateResponse.encode(message.setLayerState, writer.uint32(58).fork()).join();
    }
    if (message.toggleLayer !== undefined) {
      ToggleLayerResponse.encode(message.toggleLayer, writer.uint32(66).fork()).join();
    }
    if (message.injectKey !== undefined) {
      InjectKeyResponse.encode(message.injectKey, writer.uint32(74).fork()).join();
    }
    if (message.tapKey !== undefined) {
      TapKeyResponse.encode(message.tapKey, writer.uint32(82).fork()).join();
    }
    if (message.subscribeEvents !== undefined) {
      SubscribeEventsResponse.encode(message.subscribeEvents, writer.uint32(90).fork()).join();
    }
    if (message.getEvents !== undefined) {
      GetEventsResponse.encode(message.getEvents, writer.uint32(98).fork()).join();
    }
    if (message.clearEvents !== undefined) {
      ClearEventsResponse.encode(message.clearEvents, writer.uint32(106).fork()).join();
    }
    if (message.getLogs !== undefined) {
      GetLogsResponse.encode(message.getLogs, writer.uint32(114).fork()).join();
    }
    if (message.clearLogs !== undefined) {
      ClearLogsResponse.encode(message.clearLogs, writer.uint32(122).fork()).join();
    }
    if (message.setLogCaptureFilter !== undefined) {
      SetLogCaptureFilterResponse.encode(message.setLogCaptureFilter, writer.uint32(130).fork()).join();
    }
    if (message.setLogStreaming !== undefined) {
      SetLogStreamingResponse.encode(message.setLogStreaming, writer.uint32(138).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setStudioLockState = SetStudioLockStateResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.enterBootloader = EnterBootloaderResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reboot = RebootResponse.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.getStudioLockState = GetStudioLockStateResponse.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.getLayerState = GetLayerStateResponse.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.setLayerState = SetLayerStateResponse.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.toggleLayer = ToggleLayerResponse.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.injectKey = InjectKeyResponse.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.tapKey = TapKeyResponse.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.subscribeEvents = SubscribeEventsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.getEvents = GetEventsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.clearEvents = ClearEventsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.getLogs = GetLogsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.clearLogs = ClearLogsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.setLogCaptureFilter = SetLogCaptureFilterResponse.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.setLogStreaming = SetLogStreamingResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.setStudioLockState = (object.setStudioLockState !== undefined && object.setStudioLockState !== null)
      ? SetStudioLockStateResponse.fromPartial(object.setStudioLockState)
      : undefined;
    message.enterBootloader = (object.enterBootloader !== undefined && object.enterBootloader !== null)
      ? EnterBootloaderResponse.fromPartial(object.enterBootloader)
      : undefined;
    message.reboot = (object.reboot !== undefined && object.reboot !== null)
      ? RebootResponse.fromPartial(object.reboot)
      : undefined;
    message.getStudioLockState = (object.getStudioLockState !== undefined && object.getStudioLockState !== null)
      ? GetStudioLockStateResponse.fromPartial(object.getStudioLockState)
      : undefined;
    message.getLayerState = (object.getLayerState !== undefined && object.getLayerState !== null)
      ? GetLayerStateResponse.fromPartial(object.getLayerState)
      : undefined;
    message.setLayerState = (object.setLayerState !== undefined && object.setLayerState !== null)
      ? SetLayerStateResponse.fromPartial(object.setLayerState)
      : undefined;
    message.toggleLayer = (object.toggleLayer !== undefined && object.toggleLayer !== null)
      ? ToggleLayerResponse.fromPartial(object.toggleLayer)
      : undefined;
    message.injectKey = (object.injectKey !== undefined && object.injectKey !== null)
      ? InjectKeyResponse.fromPartial(object.injectKey)
      : undefined;
    message.tapKey = (object.tapKey !== undefined && object.tapKey !== null)
      ? TapKeyResponse.fromPartial(object.tapKey)
      : undefined;
    message.subscribeEvents = (object.subscribeEvents !== undefined && object.subscribeEvents !== null)
      ? SubscribeEventsResponse.fromPartial(object.subscribeEvents)
      : undefined;
    message.getEvents = (object.getEvents !== undefined && object.getEvents !== null)
      ? GetEventsResponse.fromPartial(object.getEvents)
      : undefined;
    message.clearEvents = (object.clearEvents !== undefined && object.clearEvents !== null)
      ? ClearEventsResponse.fromPartial(object.clearEvents)
      : undefined;
    message.getLogs = (object.getLogs !== undefined && object.getLogs !== null)
      ? GetLogsResponse.fromPartial(object.getLogs)
      : undefined;
    message.clearLogs = (object.clearLogs !== undefined && object.clearLogs !== null)
      ? ClearLogsResponse.fromPartial(object.clearLogs)
      : undefined;
    message.setLogCaptureFilter = (object.setLogCaptureFilter !== undefined && object.setLogCaptureFilter !== null)
      ? SetLogCaptureFilterResponse.fromPartial(object.setLogCaptureFilter)
      : undefined;
    message.setLogStreaming = (object.setLogStreaming !== undefined && object.setLogStreaming !== null)
      ? SetLogStreamingResponse.fromPartial(object.setLogStreaming)
      : undefined;
    return message;
  },
};

function createBaseNotification(): Notification {
  return { logStream: undefined };
}

export const Notification: MessageFns<Notification> = {
  encode(message: Notification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.logStream !== undefined) {
      LogStreamNotification.encode(message.logStream, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Notification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.logStream = LogStreamNotification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Notification>): Notification {
    return Notification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Notification>): Notification {
    const message = createBaseNotification();
    message.logStream = (object.logStream !== undefined && object.logStream !== null)
      ? LogStreamNotification.fromPartial(object.logStream)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
