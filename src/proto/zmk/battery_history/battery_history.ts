// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: zmk/battery_history/battery_history.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "zmk.battery_history";

/**
 * Request to get battery history from all devices (central and peripherals)
 * Data is returned via notifications as streaming entries
 */
export interface GetBatteryHistoryRequest {
}

/** A single battery history entry */
export interface BatteryHistoryEntry {
  /** Unix timestamp in seconds when this entry was recorded */
  timestamp: number;
  /** Battery level percentage (0-100) */
  batteryLevel: number;
}

/** Device metadata information */
export interface DeviceMetadata {
  /** Device name/identifier */
  deviceName: string;
  /** Recording interval in minutes */
  recordingIntervalMinutes: number;
  /** Maximum number of entries that can be stored */
  maxEntries: number;
}

/** Response containing battery history data */
export interface GetBatteryHistoryResponse {
}

/**
 * Request to clear battery history from device storage
 * (For future use when backend sync is implemented)
 */
export interface ClearBatteryHistoryRequest {
}

/** Response for clear operation */
export interface ClearBatteryHistoryResponse {
  /** Number of entries that were cleared */
  entriesCleared: number;
}

/** Main request message */
export interface Request {
  getHistory?: GetBatteryHistoryRequest | undefined;
  clearHistory?: ClearBatteryHistoryRequest | undefined;
}

/** Error response */
export interface ErrorResponse {
  message: string;
}

/** Main response message */
export interface Response {
  error?: ErrorResponse | undefined;
  getHistory?: GetBatteryHistoryResponse | undefined;
  clearHistory?: ClearBatteryHistoryResponse | undefined;
}

/**
 * Notification sent when battery history entry is received
 * Used for streaming battery history from peripherals
 */
export interface BatteryHistoryNotification {
  /** Source ID: 0 for central, 1+ for peripherals */
  sourceId: number;
  /** Battery history entry */
  entry:
    | BatteryHistoryEntry
    | undefined;
  /** True if this is the last entry in the batch */
  isLast: boolean;
  /** Total number of entries being sent (only set on first entry) */
  totalEntries: number;
  /** Index of this entry in the batch (0-based) */
  entryIndex: number;
}

/** Notification wrapper - sent as custom notification payload */
export interface Notification {
  batteryHistory?: BatteryHistoryNotification | undefined;
}

function createBaseGetBatteryHistoryRequest(): GetBatteryHistoryRequest {
  return {};
}

export const GetBatteryHistoryRequest: MessageFns<GetBatteryHistoryRequest> = {
  encode(_: GetBatteryHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBatteryHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBatteryHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetBatteryHistoryRequest>): GetBatteryHistoryRequest {
    return GetBatteryHistoryRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetBatteryHistoryRequest>): GetBatteryHistoryRequest {
    const message = createBaseGetBatteryHistoryRequest();
    return message;
  },
};

function createBaseBatteryHistoryEntry(): BatteryHistoryEntry {
  return { timestamp: 0, batteryLevel: 0 };
}

export const BatteryHistoryEntry: MessageFns<BatteryHistoryEntry> = {
  encode(message: BatteryHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== 0) {
      writer.uint32(8).uint32(message.timestamp);
    }
    if (message.batteryLevel !== 0) {
      writer.uint32(16).uint32(message.batteryLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatteryHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatteryHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.batteryLevel = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<BatteryHistoryEntry>): BatteryHistoryEntry {
    return BatteryHistoryEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatteryHistoryEntry>): BatteryHistoryEntry {
    const message = createBaseBatteryHistoryEntry();
    message.timestamp = object.timestamp ?? 0;
    message.batteryLevel = object.batteryLevel ?? 0;
    return message;
  },
};

function createBaseDeviceMetadata(): DeviceMetadata {
  return { deviceName: "", recordingIntervalMinutes: 0, maxEntries: 0 };
}

export const DeviceMetadata: MessageFns<DeviceMetadata> = {
  encode(message: DeviceMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceName !== "") {
      writer.uint32(10).string(message.deviceName);
    }
    if (message.recordingIntervalMinutes !== 0) {
      writer.uint32(16).uint32(message.recordingIntervalMinutes);
    }
    if (message.maxEntries !== 0) {
      writer.uint32(24).uint32(message.maxEntries);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recordingIntervalMinutes = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxEntries = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<DeviceMetadata>): DeviceMetadata {
    return DeviceMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceMetadata>): DeviceMetadata {
    const message = createBaseDeviceMetadata();
    message.deviceName = object.deviceName ?? "";
    message.recordingIntervalMinutes = object.recordingIntervalMinutes ?? 0;
    message.maxEntries = object.maxEntries ?? 0;
    return message;
  },
};

function createBaseGetBatteryHistoryResponse(): GetBatteryHistoryResponse {
  return {};
}

export const GetBatteryHistoryResponse: MessageFns<GetBatteryHistoryResponse> = {
  encode(_: GetBatteryHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBatteryHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBatteryHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetBatteryHistoryResponse>): GetBatteryHistoryResponse {
    return GetBatteryHistoryResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetBatteryHistoryResponse>): GetBatteryHistoryResponse {
    const message = createBaseGetBatteryHistoryResponse();
    return message;
  },
};

function createBaseClearBatteryHistoryRequest(): ClearBatteryHistoryRequest {
  return {};
}

export const ClearBatteryHistoryRequest: MessageFns<ClearBatteryHistoryRequest> = {
  encode(_: ClearBatteryHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearBatteryHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearBatteryHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearBatteryHistoryRequest>): ClearBatteryHistoryRequest {
    return ClearBatteryHistoryRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ClearBatteryHistoryRequest>): ClearBatteryHistoryRequest {
    const message = createBaseClearBatteryHistoryRequest();
    return message;
  },
};

function createBaseClearBatteryHistoryResponse(): ClearBatteryHistoryResponse {
  return { entriesCleared: 0 };
}

export const ClearBatteryHistoryResponse: MessageFns<ClearBatteryHistoryResponse> = {
  encode(message: ClearBatteryHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entriesCleared !== 0) {
      writer.uint32(8).uint32(message.entriesCleared);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClearBatteryHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClearBatteryHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.entriesCleared = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ClearBatteryHistoryResponse>): ClearBatteryHistoryResponse {
    return ClearBatteryHistoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClearBatteryHistoryResponse>): ClearBatteryHistoryResponse {
    const message = createBaseClearBatteryHistoryResponse();
    message.entriesCleared = object.entriesCleared ?? 0;
    return message;
  },
};

function createBaseRequest(): Request {
  return { getHistory: undefined, clearHistory: undefined };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.getHistory !== undefined) {
      GetBatteryHistoryRequest.encode(message.getHistory, writer.uint32(10).fork()).join();
    }
    if (message.clearHistory !== undefined) {
      ClearBatteryHistoryRequest.encode(message.clearHistory, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.getHistory = GetBatteryHistoryRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clearHistory = ClearBatteryHistoryRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.getHistory = (object.getHistory !== undefined && object.getHistory !== null)
      ? GetBatteryHistoryRequest.fromPartial(object.getHistory)
      : undefined;
    message.clearHistory = (object.clearHistory !== undefined && object.clearHistory !== null)
      ? ClearBatteryHistoryRequest.fromPartial(object.clearHistory)
      : undefined;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponse(): Response {
  return { error: undefined, getHistory: undefined, clearHistory: undefined };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.getHistory !== undefined) {
      GetBatteryHistoryResponse.encode(message.getHistory, writer.uint32(18).fork()).join();
    }
    if (message.clearHistory !== undefined) {
      ClearBatteryHistoryResponse.encode(message.clearHistory, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.getHistory = GetBatteryHistoryResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clearHistory = ClearBatteryHistoryResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.getHistory = (object.getHistory !== undefined && object.getHistory !== null)
      ? GetBatteryHistoryResponse.fromPartial(object.getHistory)
      : undefined;
    message.clearHistory = (object.clearHistory !== undefined && object.clearHistory !== null)
      ? ClearBatteryHistoryResponse.fromPartial(object.clearHistory)
      : undefined;
    return message;
  },
};

function createBaseBatteryHistoryNotification(): BatteryHistoryNotification {
  return { sourceId: 0, entry: undefined, isLast: false, totalEntries: 0, entryIndex: 0 };
}

export const BatteryHistoryNotification: MessageFns<BatteryHistoryNotification> = {
  encode(message: BatteryHistoryNotification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceId !== 0) {
      writer.uint32(8).uint32(message.sourceId);
    }
    if (message.entry !== undefined) {
      BatteryHistoryEntry.encode(message.entry, writer.uint32(18).fork()).join();
    }
    if (message.isLast !== false) {
      writer.uint32(24).bool(message.isLast);
    }
    if (message.totalEntries !== 0) {
      writer.uint32(32).uint32(message.totalEntries);
    }
    if (message.entryIndex !== 0) {
      writer.uint32(40).uint32(message.entryIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatteryHistoryNotification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatteryHistoryNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sourceId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entry = BatteryHistoryEntry.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isLast = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalEntries = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.entryIndex = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<BatteryHistoryNotification>): BatteryHistoryNotification {
    return BatteryHistoryNotification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatteryHistoryNotification>): BatteryHistoryNotification {
    const message = createBaseBatteryHistoryNotification();
    message.sourceId = object.sourceId ?? 0;
    message.entry = (object.entry !== undefined && object.entry !== null)
      ? BatteryHistoryEntry.fromPartial(object.entry)
      : undefined;
    message.isLast = object.isLast ?? false;
    message.totalEntries = object.totalEntries ?? 0;
    message.entryIndex = object.entryIndex ?? 0;
    return message;
  },
};

function createBaseNotification(): Notification {
  return { batteryHistory: undefined };
}

export const Notification: MessageFns<Notification> = {
  encode(message: Notification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batteryHistory !== undefined) {
      BatteryHistoryNotification.encode(message.batteryHistory, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Notification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batteryHistory = BatteryHistoryNotification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Notification>): Notification {
    return Notification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Notification>): Notification {
    const message = createBaseNotification();
    message.batteryHistory = (object.batteryHistory !== undefined && object.batteryHistory !== null)
      ? BatteryHistoryNotification.fromPartial(object.batteryHistory)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
