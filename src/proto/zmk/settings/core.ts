// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: zmk/settings/core.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "zmk.settings";

/** Activity settings (sleep/idle timeouts) */
export interface ActivitySettings {
  /** Idle timeout in milliseconds (0 to disable) */
  idleMs: number;
  /** Sleep timeout in milliseconds (0 to disable) */
  sleepMs: number;
  /**
   * Source device identifier (0 = central, 1+ = peripheral index)
   * Used to identify which device the settings came from in notifications
   */
  source: number;
}

/** Request to get activity settings */
export interface GetActivitySettingsRequest {
}

/** Response with current activity settings */
export interface GetActivitySettingsResponse {
  settings: ActivitySettings | undefined;
}

/** Request to set activity settings */
export interface SetActivitySettingsRequest {
  settings: ActivitySettings | undefined;
}

/** Response after setting activity settings */
export interface SetActivitySettingsResponse {
  success: boolean;
}

/**
 * Request to get activity settings from all devices (central + peripherals)
 * This triggers devices to report their settings via notifications
 */
export interface GetAllActivitySettingsRequest {
}

/**
 * Response confirming the request was sent
 * Actual settings will be delivered via ActivitySettingsNotification
 */
export interface GetAllActivitySettingsResponse {
  requestSent: boolean;
}

/**
 * Notification message sent when a device reports its activity settings
 * This is sent from both central and peripherals in response to GetAllActivitySettingsRequest
 */
export interface ActivitySettingsNotification {
  settings: ActivitySettings | undefined;
}

/** Main request message - extensible for future settings */
export interface Request {
  getActivitySettings?: GetActivitySettingsRequest | undefined;
  setActivitySettings?: SetActivitySettingsRequest | undefined;
  getAllActivitySettings?: GetAllActivitySettingsRequest | undefined;
}

/** Error response for any failed request */
export interface ErrorResponse {
  message: string;
}

/** Main response message - extensible for future settings */
export interface Response {
  error?: ErrorResponse | undefined;
  getActivitySettings?: GetActivitySettingsResponse | undefined;
  setActivitySettings?: SetActivitySettingsResponse | undefined;
  getAllActivitySettings?: GetAllActivitySettingsResponse | undefined;
}

/** Notification message - sent asynchronously from firmware to web UI */
export interface Notification {
  activitySettings?: ActivitySettingsNotification | undefined;
}

function createBaseActivitySettings(): ActivitySettings {
  return { idleMs: 0, sleepMs: 0, source: 0 };
}

export const ActivitySettings: MessageFns<ActivitySettings> = {
  encode(message: ActivitySettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idleMs !== 0) {
      writer.uint32(8).uint32(message.idleMs);
    }
    if (message.sleepMs !== 0) {
      writer.uint32(16).uint32(message.sleepMs);
    }
    if (message.source !== 0) {
      writer.uint32(24).uint32(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActivitySettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActivitySettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.idleMs = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sleepMs = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.source = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ActivitySettings>): ActivitySettings {
    return ActivitySettings.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ActivitySettings>): ActivitySettings {
    const message = createBaseActivitySettings();
    message.idleMs = object.idleMs ?? 0;
    message.sleepMs = object.sleepMs ?? 0;
    message.source = object.source ?? 0;
    return message;
  },
};

function createBaseGetActivitySettingsRequest(): GetActivitySettingsRequest {
  return {};
}

export const GetActivitySettingsRequest: MessageFns<GetActivitySettingsRequest> = {
  encode(_: GetActivitySettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActivitySettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActivitySettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetActivitySettingsRequest>): GetActivitySettingsRequest {
    return GetActivitySettingsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetActivitySettingsRequest>): GetActivitySettingsRequest {
    const message = createBaseGetActivitySettingsRequest();
    return message;
  },
};

function createBaseGetActivitySettingsResponse(): GetActivitySettingsResponse {
  return { settings: undefined };
}

export const GetActivitySettingsResponse: MessageFns<GetActivitySettingsResponse> = {
  encode(message: GetActivitySettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settings !== undefined) {
      ActivitySettings.encode(message.settings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActivitySettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetActivitySettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.settings = ActivitySettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetActivitySettingsResponse>): GetActivitySettingsResponse {
    return GetActivitySettingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetActivitySettingsResponse>): GetActivitySettingsResponse {
    const message = createBaseGetActivitySettingsResponse();
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? ActivitySettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseSetActivitySettingsRequest(): SetActivitySettingsRequest {
  return { settings: undefined };
}

export const SetActivitySettingsRequest: MessageFns<SetActivitySettingsRequest> = {
  encode(message: SetActivitySettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settings !== undefined) {
      ActivitySettings.encode(message.settings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetActivitySettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetActivitySettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.settings = ActivitySettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetActivitySettingsRequest>): SetActivitySettingsRequest {
    return SetActivitySettingsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetActivitySettingsRequest>): SetActivitySettingsRequest {
    const message = createBaseSetActivitySettingsRequest();
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? ActivitySettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseSetActivitySettingsResponse(): SetActivitySettingsResponse {
  return { success: false };
}

export const SetActivitySettingsResponse: MessageFns<SetActivitySettingsResponse> = {
  encode(message: SetActivitySettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetActivitySettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetActivitySettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<SetActivitySettingsResponse>): SetActivitySettingsResponse {
    return SetActivitySettingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetActivitySettingsResponse>): SetActivitySettingsResponse {
    const message = createBaseSetActivitySettingsResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseGetAllActivitySettingsRequest(): GetAllActivitySettingsRequest {
  return {};
}

export const GetAllActivitySettingsRequest: MessageFns<GetAllActivitySettingsRequest> = {
  encode(_: GetAllActivitySettingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAllActivitySettingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAllActivitySettingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetAllActivitySettingsRequest>): GetAllActivitySettingsRequest {
    return GetAllActivitySettingsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetAllActivitySettingsRequest>): GetAllActivitySettingsRequest {
    const message = createBaseGetAllActivitySettingsRequest();
    return message;
  },
};

function createBaseGetAllActivitySettingsResponse(): GetAllActivitySettingsResponse {
  return { requestSent: false };
}

export const GetAllActivitySettingsResponse: MessageFns<GetAllActivitySettingsResponse> = {
  encode(message: GetAllActivitySettingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestSent !== false) {
      writer.uint32(8).bool(message.requestSent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAllActivitySettingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAllActivitySettingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestSent = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetAllActivitySettingsResponse>): GetAllActivitySettingsResponse {
    return GetAllActivitySettingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetAllActivitySettingsResponse>): GetAllActivitySettingsResponse {
    const message = createBaseGetAllActivitySettingsResponse();
    message.requestSent = object.requestSent ?? false;
    return message;
  },
};

function createBaseActivitySettingsNotification(): ActivitySettingsNotification {
  return { settings: undefined };
}

export const ActivitySettingsNotification: MessageFns<ActivitySettingsNotification> = {
  encode(message: ActivitySettingsNotification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.settings !== undefined) {
      ActivitySettings.encode(message.settings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActivitySettingsNotification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActivitySettingsNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.settings = ActivitySettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ActivitySettingsNotification>): ActivitySettingsNotification {
    return ActivitySettingsNotification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ActivitySettingsNotification>): ActivitySettingsNotification {
    const message = createBaseActivitySettingsNotification();
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? ActivitySettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseRequest(): Request {
  return { getActivitySettings: undefined, setActivitySettings: undefined, getAllActivitySettings: undefined };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.getActivitySettings !== undefined) {
      GetActivitySettingsRequest.encode(message.getActivitySettings, writer.uint32(10).fork()).join();
    }
    if (message.setActivitySettings !== undefined) {
      SetActivitySettingsRequest.encode(message.setActivitySettings, writer.uint32(18).fork()).join();
    }
    if (message.getAllActivitySettings !== undefined) {
      GetAllActivitySettingsRequest.encode(message.getAllActivitySettings, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.getActivitySettings = GetActivitySettingsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.setActivitySettings = SetActivitySettingsRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.getAllActivitySettings = GetAllActivitySettingsRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Request>): Request {
    return Request.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Request>): Request {
    const message = createBaseRequest();
    message.getActivitySettings = (object.getActivitySettings !== undefined && object.getActivitySettings !== null)
      ? GetActivitySettingsRequest.fromPartial(object.getActivitySettings)
      : undefined;
    message.setActivitySettings = (object.setActivitySettings !== undefined && object.setActivitySettings !== null)
      ? SetActivitySettingsRequest.fromPartial(object.setActivitySettings)
      : undefined;
    message.getAllActivitySettings =
      (object.getAllActivitySettings !== undefined && object.getAllActivitySettings !== null)
        ? GetAllActivitySettingsRequest.fromPartial(object.getAllActivitySettings)
        : undefined;
    return message;
  },
};

function createBaseErrorResponse(): ErrorResponse {
  return { message: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    error: undefined,
    getActivitySettings: undefined,
    setActivitySettings: undefined,
    getAllActivitySettings: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== undefined) {
      ErrorResponse.encode(message.error, writer.uint32(10).fork()).join();
    }
    if (message.getActivitySettings !== undefined) {
      GetActivitySettingsResponse.encode(message.getActivitySettings, writer.uint32(18).fork()).join();
    }
    if (message.setActivitySettings !== undefined) {
      SetActivitySettingsResponse.encode(message.setActivitySettings, writer.uint32(26).fork()).join();
    }
    if (message.getAllActivitySettings !== undefined) {
      GetAllActivitySettingsResponse.encode(message.getAllActivitySettings, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = ErrorResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.getActivitySettings = GetActivitySettingsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.setActivitySettings = SetActivitySettingsResponse.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.getAllActivitySettings = GetAllActivitySettingsResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Response>): Response {
    return Response.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Response>): Response {
    const message = createBaseResponse();
    message.error = (object.error !== undefined && object.error !== null)
      ? ErrorResponse.fromPartial(object.error)
      : undefined;
    message.getActivitySettings = (object.getActivitySettings !== undefined && object.getActivitySettings !== null)
      ? GetActivitySettingsResponse.fromPartial(object.getActivitySettings)
      : undefined;
    message.setActivitySettings = (object.setActivitySettings !== undefined && object.setActivitySettings !== null)
      ? SetActivitySettingsResponse.fromPartial(object.setActivitySettings)
      : undefined;
    message.getAllActivitySettings =
      (object.getAllActivitySettings !== undefined && object.getAllActivitySettings !== null)
        ? GetAllActivitySettingsResponse.fromPartial(object.getAllActivitySettings)
        : undefined;
    return message;
  },
};

function createBaseNotification(): Notification {
  return { activitySettings: undefined };
}

export const Notification: MessageFns<Notification> = {
  encode(message: Notification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activitySettings !== undefined) {
      ActivitySettingsNotification.encode(message.activitySettings, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Notification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.activitySettings = ActivitySettingsNotification.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Notification>): Notification {
    return Notification.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Notification>): Notification {
    const message = createBaseNotification();
    message.activitySettings = (object.activitySettings !== undefined && object.activitySettings !== null)
      ? ActivitySettingsNotification.fromPartial(object.activitySettings)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
